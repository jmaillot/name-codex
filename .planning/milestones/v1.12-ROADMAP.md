# Roadmap: Name Codex

## Milestones

- ✅ **v1.0 Baseline Release** — Phase 1 (shipped 2026-08-19)
- ✅ **v1.1 Quality & Refactor** — Phases 2-6 (shipped 2026-08-21)
- ✅ **v1.2 Performance** — Phase 7 (shipped 2026-08-21)
- ✅ **v1.3 Developer Portal UI** — Phases 8-10 (shipped 2026-08-24)
- ✅ **v1.4 Light Mode** — Phases 11-12 (shipped 2026-08-24)
- ✅ **v1.5 Naming Convention Expansion** — Phases 13-15 (shipped 2026-08-25)
- ✅ **v1.6 Data Integrity** — Phases 16-18 (shipped 2026-08-28)
- ✅ **v1.7 Constraint Wiring & CAF Expansion** — Phases 19-20 (shipped 2026-08-31)
- ✅ **v1.8 Data Tier + Observability + Network Edge** — Phases 21-23 (shipped 2026-09-01, 59 conventions, Azure 27, 128 files PASS)
- ✅ **v1.9 Taxonomy and Description Fixes** — Post-v1.8 fixes (shipped 2026-09-01, 59 conventions, 135 files PASS, 895 parity, 240 tests) — APIM/AGW/APPI/ADF/EVH/LB/LOG/SQLDB/SQLSRV taxonomy + description expansion for all categories + validation for all categories
- ✅ **v1.10 Governance Insights** — Phases 24-25 (shipped 2026-09-01, 59 conventions, 135 files PASS, 911 parity, 240 tests)
- ✅ **v1.11 CAF Expansion II — Infra + Data/AI** — Phases 26-27 (shipped 2026-09-01, 65 conventions, Azure 33, 147 files PASS, 868 parity, 240 tests)
- ✅ **v1.12 CAF Expansion III** — Phases 28-29 (shipped 2026-09-03, 65→71 conventions, Azure 33→39, Region-based lowercase + dotted DNS)

## Phases

<details>
<summary>✅ v1.0 Baseline Release (Phase 1) — SHIPPED 2026-08-19</summary>

- [x] Phase 1: Baseline Release v1.0 (3/3 plans) — completed 2026-08-19

Full details: `.planning/milestones/v1.0-ROADMAP.md`

</details>

<details>
<summary>✅ v1.1 Quality & Refactor (Phases 2-6) — SHIPPED 2026-08-21</summary>

- [x] Phase 2: Backup & Safety Net (3/3 plans) — completed 2026-08-20
- [x] Phase 3: Refactor App.tsx (2/2 plans) — completed 2026-08-20
- [x] Phase 4: Bug Fixes (5/5 plans) — completed 2026-08-20
- [x] Phase 5: Tests (2/2 plans) — completed 2026-08-21
- [x] Phase 6: Cleanup (2/2 plans) — completed 2026-08-21

Full details: `.planning/milestones/v1.1-ROADMAP.md`

</details>

<details>
<summary>✅ v1.2 Performance (Phase 7) — SHIPPED 2026-08-21</summary>

- [x] Phase 7: Performance Optimizations (3/3 plans) — completed 2026-08-21

Full details: `.planning/milestones/v1.2-ROADMAP.md`

</details>

<details>
<summary>✅ v1.3 Developer Portal UI (Phases 8-10) — SHIPPED 2026-08-24</summary>

- [x] Phase 8: Design Token Foundation (3/3 plans) — completed 2026-08-23
- [x] Phase 9: Layout Restructure (3/3 plans) — completed 2026-08-23
- [x] Phase 10: Developer-Portal Polish & Verification (4/4 plans incl. 2 gap-closure) — completed 2026-08-24

Full details: `.planning/milestones/v1.3-ROADMAP.md`

</details>

<details>
<summary>✅ v1.4 Light Mode (Phases 11-12) — SHIPPED 2026-08-24</summary>

- [x] Phase 11: Light Token Palette (2/2 plans) — completed 2026-08-24
- [x] Phase 12: Theme Switching & Verification (3/3 plans) — completed 2026-08-24

Full details: `.planning/milestones/v1.4-ROADMAP.md`

</details>

<details>
<summary>✅ v1.5 Naming Convention Expansion (Phases 13-15) — SHIPPED 2026-08-25</summary>

- [x] Phase 13: New Microsoft 365 Categories — Teams, SharePoint, Power Platform, Purview (4/4 plans) — completed 2026-08-25
- [x] Phase 14: Category Expansions — Azure CAF Resources + Intune (5/5 plans) — completed 2026-08-25
- [x] Phase 15: Milestone Verification — Full Catalog Integrity (4/4 plans) — completed 2026-08-25

Catalog grew 25→45 conventions across 7→11 categories, data-driven JSON only; builder correctness sweep; 183 tests incl. catalog-integrity proofs; verdict COMPLETE-WITH-WAIVER. Phases archived to `.planning/milestones/v1.5-phases/`.

Full details: `.planning/milestones/v1.5-ROADMAP.md`

</details>

<details>
<summary>✅ v1.6 Data Integrity (Phases 16-18) — SHIPPED 2026-08-28</summary>

- [x] Phase 16: Data Validation Gate — check:data + CI (3/3 plans) — completed 2026-08-27
- [x] Phase 17: Generic Segment Constraint Vocabulary (2/2 plans) — completed 2026-08-28
- [x] Phase 18: Region Catalog Expansion (0/0 plans, direct impl + UAT 7/7) — completed 2026-08-28 — 40 short codes AUE..WUS3 + 40 Country ISO codes AE..ZA, check:data 96 PASS, vitest 226 PASS, threats_open 0

Shipped: zero-dep check:data gate (validateRule/Segment/Generator + CI data job), generic constraints vocabulary (allowedPattern/minLength/maxLength) with bilingual DATA-FORMAT docs, and Region+Country 4→40 expansion proving the gate (short-code per user approval, UAT verified). Full details: `.planning/milestones/v1.6-ROADMAP.md`

</details>

### ✅ v1.7 Constraint Wiring & CAF Expansion (Phases 19-20) — SHIPPED 2026-08-31

**Goal:** Wire the generic segment constraint vocabulary into builder enforcement and expand the Azure CAF catalog with six high-demand conventions — making constraints visible to users and conventions data-driven.

Shipped: 50 conventions (111 files, EN 628 FR 628), Service Bus/AKS/VNet GW grouped with sub-objects, Container Registry hyphen-free, validation real limits for 9 lenient objects, all gates green.

#### Phase 19: Constraint Wiring (WIRE-01)

- [x] **Status:** Shipped 2026-08-31 (commit de44964)
- **Requirements:** WIRE-01
- **Goal:** Make declared `SegmentConstraints` live — type extension, dropdown option filtering via `optionsForSegment`, length/pattern validation rows via `validateName`, reactive clamp and `maxLength` UI via `useAppState`/`SegmentEditor`, and docs flip from future-scope to live.

| # | Deliverable | Success Criteria |
|---|-------------|------------------|
| 19a | Type extension + option filtering | 1. User opens a convention using Region, Environment, or Instance — dropdown lists only show values matching the segment's `allowedPattern` (violating options hidden, yet Custom input remains available) — `optionsForSegment` reads `SegmentConstraints` via `fieldByName`/`getSegmentFile` with try/catch and `filtered.length>0 ? filtered : original` guard. |
| 19b | Validation rows + governance score | 2. User types a custom value violating `allowedPattern`, `minLength`, or `maxLength` — a distinct validation row appears (pattern/min/max) with inline hint/error styling and the governance score drops accordingly (separate from existing `field.allowedPattern`/`rule.maxLength` rows, `""` sentinel bypassed). |
| 19c | Reactive clamp + UI | 3. User pastes or types beyond `maxLength` in a text/custom field — the input is clamped via `maxLength` attribute + `slice(0, maxLength)` on commit, the clamped value is what renders/copies, while validation still flags the raw over-length value before clamp. |
| 19d | Docs + seed | 4. User (contributor) reads `docs/DATA-FORMAT.md` / `_FR.md` and sees constraints documented as live since v1.7 (future-scope note removed) with example `constraints` seeded on at least one core library (e.g. `core/region` or `core/instance`) and `check:data` + `build` + `vitest` remain green. |

**Depends on:** v1.6 gate and vocabulary (inert `SegmentConstraints` type + `check:data` declaration validation).

**Plans:** TBD by plan-phase (coarse granularity — type extension → filtering → validation rows → clamp/UI → docs flip batched).

#### Phase 20: CAF Catalog Expansion (CAF-01..06)

- [x] **Status:** Shipped 2026-08-31 (50 conventions, 18 Azure, 107 files, 490 EN/FR)
- **Requirements:** CAF-01, CAF-02, CAF-03, CAF-04, CAF-05, CAF-06
- **Goal:** Expand the Azure CAF catalog with six high-demand conventions as pure data-driven JSON — each with pattern/fields/builder/validation/examples, optional per-resource workload library, and EN/FR parity — growing the catalog 44→50 with all gates green.

| # | Deliverable | Success Criteria |
|---|-------------|------------------|
| 20a | Six CAF conventions as JSON | 1. User can select each new Azure convention in category navigation — Container Registry `acr` (`acr-[Environment]-[Region]-[Instance]`), AKS `aks` (`aks-[Environment]-[Region]-[Workload]-[Instance]`), Cosmos DB `cosmos` (`cosmos-[Workload]-[Environment]-[Region]-[Instance]` workload-first), Service Bus `sb`, Function App `func`, App Service `app` — and generate a name matching its CAF pattern and `validation` (globally-unique lowercase `^[a-z0-9…]$` for `acr`/`func`/`app` where applicable). |
| 20b | Workload libraries + constraints | 2. User picking `aks`/`cosmos`/other Workload-bearing conventions sees a constrained Workload dropdown (where a per-resource `azure/*-workload.json` library exists, filtered by any `constraints.allowedPattern` via Phase 19 wiring) with meaningful taxonomy values. |
| 20c | Bilingual + catalog integrity | 3. User toggles EN↔FR — every new `data.rule.*` and `data.lib.*` key renders translated (EN/FR parity holds, e.g. ~380=380), catalog counts read 51 conventions across 11 categories (45→51) auto-discovered via `import.meta.glob` with no code diff beyond Phase 19 wiring. |
| 20d | Gates green | 4. `npm run check:data` passes on ~102 files, `npm run build` + `npm run lint` + `npm test` (vitest ~232 tests incl. updated 51-count/11-category/search-reachability/EN=FR parity proofs) all green, and CI `needs [build,lint,test,data]` remains intact. |

**Depends on:** Phase 19 type extension (W-01) so new workload libs can carry `constraints`; otherwise parallelizable after Phase 19 types land.

**Plans:** TBD by plan-phase (coarse granularity — six rule files + workload libs + locale keys batched, sequential for review or parallel after W-01).

### ✅ v1.8 Data Tier + Observability + Network Edge (Phases 21-23) — SHIPPED 2026-09-01 (59 conventions, Azure 27, 11 categories, 128 files PASS)

**Goal:** Expand Azure CAF catalog with 10 high-demand data/network conventions + department taxonomy — all data-driven JSON, no code. Shipped: Phase 21 (sql/sqlsrv/apim/adf 53), Phase 22 (log/appi/evh 56), Phase 23 (lb/agw/afw + dept 5→9 59). Gates green.

#### Phase 21: Data Tier — SQL + API Management + Data Factory (SQL-01..04)

- [x] **Status:** Shipped 2026-09-01 (53 conventions, Azure 21, 115 files PASS, 238 tests) — validated #1
- **Requirements:** SQL-01 (sql-server), SQL-02 (sql-db), APIM-01 (api-mgmt), ADF-01 (data-factory)
- **Goal:** Ship `sqlsrv`/`sql` pair + `apim` + `adf` as CAF `sqlsrv/sql-[Workload]-[Env]-[Region]-[Instance]`, `apim-...`, `adf-...` with workload libs.

| # | Deliverable | Success Criteria |
|---|-------------|------------------|
| 21a | Four data-tier conventions | SQL Server `sqlsrv-...` + SQL DB `sql-...` + API Management `apim-...` + Data Factory `adf-...` selectable in Azure nav, generate `^[a-z0-9-]` lowercase, `maxLength` 63/50. |
| 21b | Workload libs | Per-resource `azure/sql-workload.json` etc. filtered via `getConstraintsForField`. |
| 21c | Bilingual + gates | EN/FR parity, `check:data` ~115 PASS, `build/lint/test` green (53→57). |

#### Phase 22: Observability — Log Analytics + App Insights + Event Hub (OBS-01..03)

- [x] **Status:** Shipped 2026-09-01 (56 conventions, Azure 24, 125 files PASS, 238 tests)
- **Requirements:** OBS-01 (log-analytics `log`), OBS-02 (app-insights `appi`), OBS-03 (event-hub `evh`)
- **Goal:** Ship `log`/`appi`/`evh` as `log/appi/evh-[Workload]-[Env]-[Region]-[Instance]` (evh hub `evh-[Purpose]`).

| # | Deliverable | Success Criteria |
|---|-------------|------------------|
| 22a | Three observability conventions | `log`, `appi`, `evh` selectable, `maxLength` 63 (log/appi) / 50 (evh namespace+hub strict), lowercase `^(?!.*--)` no --, filtered workload libs via getConstraintsForField — actual 53→56 (roadmap shorthand 57→60, Phase 21 landed 53). |
| 22b | Gates | 125 files PASS, 805=805 parity, build/lint/tsc green, 238 tests. |

#### Phase 23: Network Edge + Department Taxonomy (NET-01..03 + DEPT-01)

- [x] **Status:** Shipped 2026-09-01 (59 conventions, Azure 27, 128 files PASS, 238 tests, parity 816=816)
- **Requirements:** NET-01 (lb), NET-02 (agw), NET-03 (afw), DEPT-01 (department 5→9)
- **Goal:** Ship `lb`/`agw`/`afw` (`lb/agw/afw-[Workload]-[Environment]-[Region]-[Instance]` 80 strict lowercase) + expand `src/data/segments/core/department.json` 5→9 with `LEGAL`/`SALES`/`SECURITY`/`RESEARCH` (+Marketing, dept 5→9).

| # | Deliverable | Success Criteria |
|---|-------------|------------------|
| 23a | Three network conventions | `lb`/`agw`/`afw` selectable, 80 strict `^(?!.*--)[a-z][a-z0-9-]{2,78}[a-z0-9]$` forceLowercase, inline Workload WEB/APP/API/SHARED — actual 56→59 (roadmap shorthand 60→63, Phase 22 landed 56; department is not a convention). |
| 23b | Department | `core/department` 5→9, `data.lib.core/department.*` EN/FR 9 keys (Marketing+IT/HR/FIN/OPS+LEGAL/SALES/SECURITY/RESEARCH) parity 816=816. |
| 23c | Gates | 128 files PASS, build/lint/tsc green, 238 tests — milestone v1.8 shipped. |

---

### ✅ v1.9 Taxonomy and Description Fixes — SHIPPED 2026-09-01 (59 conventions, 135 files PASS, 895 parity, 240 tests)

Post-v1.8 fixes applied directly on `main` (59 conventions, 135 files, 240 tests): APIM `APIM-[Purpose]-[Environment]` 7, AGW 8, APPI 13, ADF 14, EVH `EVHNS`+`EVH` grouped, LB grouped `lbi`/`lbe`, LOG 8, SQLDB 10, SQLSRV 11; descriptions expanded for all 11 categories (Azure 13 + non-Azure 17) and `Pattern` removed from all JSON/locales; validation expanded for all categories (check:data `description` ≥50/≥30, no pattern; data-integrity 20 tests). Tags `v1.9` at `a882674` (later `5537a89` for checks, `0b796e4` for Power Platform/Intune).

### ✅ v1.10 Governance Insights — Phases 24-25 (shipped 2026-09-01, 59 conventions, 135 files PASS, 911 parity, 240 tests)

**Goal:** Make governance actionable — richer score breakdown, exportable policy documentation, and compliance checklist for copy-paste governance.

#### Phase 24: Governance Insights — Actionable Hints and Exports (GOV-01..03)

- [x] **Status:** Shipped 2026-09-01 (commits 389b46d..4c1a355, 6 tasks, 2 plans)
- **Requirements:** GOV-01 (actionable hints), GOV-02 (Markdown export), GOV-03 (JSON export)
- **Goal:** User sees actionable hints for each failing governance rule and can export Markdown/JSON policy documentation via Copy actions.

| # | Deliverable | Success Criteria |
|---|-------------|------------------|
| 24a | Actionable hints | User sees a hint like "Add Environment" or "Fix double hyphen" for each failing validation row, with navigation to the segment |
| 24b | Markdown export | User clicks Copy Markdown and gets pattern, segments, validation results, and score |
| 24c | JSON export | User can export structured JSON (convention id, pattern, segments, validation config, score) for automation |

#### Phase 25: Search & Navigation (NAV-01)

- [x] **Status:** Shipped 2026-09-01 (1 plan, 2 tasks, commits 9fac778..4c1a355, 911 parity, 240 tests)
- **Requirements:** NAV-01 (search and filtering)
- **Goal:** User can quickly discover conventions via global search and category filtering with live results.

| # | Deliverable | Success Criteria |
|---|-------------|------------------|
| 25a | Search and filtering | User types in global search and sees filtered conventions by name, description, id, or pattern fragment with live results |

---

### ✅ v1.11 CAF Expansion II — Infra + Data/AI (Phases 26-27) — SHIPPED 2026-09-01 (65 conventions, Azure 33, 147 files PASS, 868 parity, 240 tests)

**Goal:** Expand Azure CAF catalog with six high-demand infra + data/AI conventions as pure data-driven JSON — Redis, Bastion, Recovery Vault, NAT Gateway, Synapse, Cognitive Services — 59→65 (Azure 27→33), EN/FR parity, all gates green. Shipped: Phase 26 (redis/bas/rsv/ngw 63, Azure 31), Phase 27 (synw/cog 65, Azure 33), 2 phases, 2 plans, 6 tasks, same-day ship.

#### Phase 26: Infrastructure — Redis + Bastion + Recovery Vault + NAT Gateway (CAF-07..10)

- [x] **Status:** Shipped 2026-09-01 (65→63? actually 59→63, Azure 27→31, 143 files PASS) — validated
- **Requirements:** CAF-07 (redis), CAF-08 (bas), CAF-09 (rsv), CAF-10 (ngw)
- **Goal:** Ship `redis`/`bas`/`rsv`/`ngw` as `redis/bas/rsv/ngw-[Workload]-[Environment]-[Region]-[Instance]` with workload libs and CAF-compliant validation.

| # | Deliverable | Success Criteria |
|---|-------------|------------------|
| 26a | Four infra conventions as JSON | 1. User can select each Azure convention in category nav — Redis `redis` (`redis-[Workload]-[Environment]-[Region]-[Instance]`), Bastion `bas`, Recovery Vault `rsv`, NAT Gateway `ngw` — and generate a name matching its CAF pattern and `validation` (lowercase `^(?!.*--)[a-z0-9-]` where applicable, correct `maxLength`). |
| 26b | Workload libs + constraints | 2. User picking `redis`/`rsv`/other Workload-bearing conventions sees a constrained Workload dropdown where a per-resource `azure/<id>-workload.json` library exists, filtered by any `constraints.allowedPattern` via Phase 19 wiring. |
| 26c | Bilingual + catalog integrity | 3. User toggles EN↔FR — every new `data.rule.*` and `data.lib.*` key renders translated (EN/FR parity holds), catalog counts read 63 conventions (59→63) auto-discovered via `import.meta.glob` with no code diff beyond Phase 19 wiring. |
| 26d | Gates green | 4. `npm run check:data` passes on ~139 files, `npm run build` + `npm run lint` + `npm test` (vitest ~244 tests incl. updated 63-count/33-Azure/search-reachability/EN=FR parity proofs) all green, CI `needs [build,lint,test,data]` intact. |

**Depends on:** Phase 19 wiring (WIRE-01) for constraint-filtered workload libs; otherwise independent.

**Plans:** 1/1 plans complete

#### Phase 27: Data Platform — Synapse + Cognitive Services (CAF-11..12)

- [x] **Status:** Shipped 2026-09-01 (63→65, Azure 31→33, 147 files PASS) — validated
- **Requirements:** CAF-11 (synw), CAF-12 (cog)
- **Goal:** Ship `synw`/`cog` as `synw/cog-[Workload]-[Environment]-[Region]-[Instance]` with workload libs, closing v1.11 at 65 conventions.

| # | Deliverable | Success Criteria |
|---|-------------|------------------|
| 27a | Two data/AI conventions as JSON | 1. User can select Synapse `synw` and Cognitive Services `cog` in Azure nav and generate a name matching `synw/cog-[Workload]-[Environment]-[Region]-[Instance]` CAF pattern and `validation` (lowercase, correct `maxLength`/global uniqueness where applicable). |
| 27b | Workload libs + constraints | 2. User picking `synw`/`cog` sees a constrained Workload dropdown where `azure/<id>-workload.json` exists, filtered via `getConstraintsForField`. |
| 27c | Bilingual + catalog integrity | 3. User toggles EN↔FR — every new `data.rule.*` and `data.lib.*` key renders translated (EN/FR parity holds), catalog counts read 65 conventions across 11 categories (63→65, Azure 31→33) auto-discovered via `import.meta.glob` with no code diff beyond Phase 19 wiring. |
| 27d | Gates green + milestone verification | 4. `npm run check:data` passes on ~141 files, `npm run build` + `npm run lint` + `npm test` (vitest ~246 tests incl. updated 65-count/11-category/EN=FR parity proofs) all green — milestone v1.11 shipped. |

**Depends on:** Phase 26 (sequential for catalog count proofs, or parallel after Phase 26 files land).

**Plans:** 1/1 plans complete

---

### ✅ v1.12 CAF Expansion III (Phases 28-29) — SHIPPED 2026-09-03 (71 conventions, Azure 39, 159 files PASS, 1048 parity, 240 tests)

**Goal:** Expand Azure CAF catalog with six additional high-demand conventions as pure data-driven JSON — EN/FR parity, Region-based lowercase patterns (dnsz dotted-domain rework), all gates green.

Shipped: 6 conventions (bvault/adb/mlw + afd/pe/dnsz) as pure JSON with 6 workload libs, EN/FR parity, 159 files gated, 240 tests; catalog 65→71 (Azure 33→39); dnsz reworked to `dnsz-[Workload]-[Environment].[Domain]` dotted-domain (Region removed, maxLength 63) per CR-01 fix, all review findings resolved.

#### Phase 28: Backup Vault + Databricks + Third Resource (CAF-13..15)

- **Status:** Shipped 2026-09-02 (68 conventions, Azure 36, 153 files PASS, 240 tests) — mlw as CAF-15 third pick
- **Requirements:** CAF-13 (bvault), CAF-14 (adb), CAF-15 (mlw)
- **Goal:** Ship `bvault`/`adb`/`mlw` as `bvault/adb/mlw-[Workload]-[Region]-[Environment]` lowercase with workload libs.

| # | Deliverable | Success Criteria |
|---|-------------|------------------|
| 28a | Three conventions as JSON | 1. User can select each Azure convention in category nav — Backup Vault `bvault`, Databricks `adb`, third `prefix` — and generate a name matching its CAF pattern and `validation` (lowercase `^(?!.*--)[a-z0-9-]` where applicable, correct `maxLength`). |
| 28b | Workload libs + constraints | 2. User picking Workload-bearing conventions sees a constrained Workload dropdown where `azure/<id>-workload.json` exists, filtered via `getConstraintsForField`. |
| 28c | Bilingual + catalog integrity | 3. User toggles EN↔FR — every new `data.rule.*` and `data.lib.*` key renders translated (EN/FR parity holds), catalog counts read 68 conventions (65→68) auto-discovered via `import.meta.glob` with no code diff beyond Phase 19 wiring. |
| 28d | Gates green | 4. `npm run check:data` passes on ~150 files, `npm run build` + `npm run lint` + `npm test` (vitest updated 68-count/39-Azure/search-reachability/EN=FR parity proofs) all green. |

**Depends on:** Phase 19 wiring (WIRE-01) for constraint-filtered workload libs; otherwise independent.

**Plans:** 1/1 plans complete

Plans:
- [x] 28-01-PLAN.md — Ship bvault/adb/mlw (azure-backup-vault + azure-databricks-workspace + azure-machine-learning-workspace) as bvault/adb/mlw-[Workload]-[Region]-[Environment] lowercase with 3 workload libs, EN/FR parity, catalog 65→68 Azure 33→36 (CAF-15 = mlw chosen as third backlog pick)

#### Phase 29: Fourth + Fifth + Sixth Resources (CAF-16..18)

- **Status:** Complete 2026-09-02
- **Requirements:** CAF-16, CAF-17, CAF-18
- **Goal:** Ship three remaining CAF conventions as `prefix-[Workload]-[Region]-[Environment]` lowercase (dnsz reworked to `dnsz-[Workload]-[Environment].[Domain]` dotted-domain per CR-01), closing v1.12 at 71 conventions.

| # | Deliverable | Success Criteria |
|---|-------------|------------------|
| 29a | Three conventions as JSON | 1. User can select each remaining Azure convention and generate a name matching its CAF pattern and `validation` (lowercase, correct `maxLength`). |
| 29b | Workload libs + constraints | 2. User picking Workload-bearing conventions sees a constrained Workload dropdown where `azure/<id>-workload.json` exists, filtered via `getConstraintsForField`. |
| 29c | Bilingual + catalog integrity | 3. User toggles EN↔FR — every new `data.rule.*` and `data.lib.*` key renders translated (EN/FR parity holds), catalog counts read 71 conventions (68→71, Azure 39) auto-discovered via `import.meta.glob` with no code diff beyond Phase 19 wiring. |
| 29d | Gates green + milestone verification | 4. `npm run check:data` passes on ~153 files, `npm run build` + `npm run lint` + `npm test` (vitest updated 71-count/39-Azure/EN=FR parity proofs) all green — milestone v1.12 shipped. |

**Depends on:** Phase 28 (sequential for catalog count proofs, or parallel after Phase 28 files land).

**Plans:** 1/1 plans complete

---

_Requirements coverage: 7/7 REQ-IDs mapped — Phase 19 (WIRE-01), Phase 20 (CAF-01..06). See traceability table in `.planning/REQUIREMENTS.md`._

_Milestone v1.7: 2 phases (19-20) | 7 requirements mapped | All covered._
_Milestone v1.8: 3 phases (21-23) | 11 requirements mapped | Shipped 2026-09-01 (53→56→59, Azure 21→24→27)._
_Milestone v1.9: Post-v1.8 fixes | 0 phases (direct fixes) | Shipped 2026-09-01 (59 conventions, 135 files)._
_Milestone v1.10: 2 phases (24-25) | 4 requirements mapped | Shipped 2026-09-01 (GOV-01..03 + NAV-01)._
_Milestone v1.11: 2 phases (26-27) | 6 requirements mapped | All covered — catalog 59→65 (Azure 27→33)._
_Milestone v1.12: 2 phases (28-29) | 6 requirements mapped | Shipped 2026-09-03 (71 conventions, Azure 39, 159 files PASS, 1048 parity, 240 tests)._

_Previous milestones archived under .planning/milestones/ (v1.0–v1.6). v1.7 to be archived on next close._
