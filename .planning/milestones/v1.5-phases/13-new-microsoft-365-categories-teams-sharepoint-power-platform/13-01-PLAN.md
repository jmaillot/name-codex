---
phase: 13-new-microsoft-365-categories-teams-sharepoint-power-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/rules/teams/team-project.json
  - src/rules/teams/team-department.json
  - src/rules/teams/team-service.json
  - src/rules/teams/m365-group-naming-policy.json
  - src/rules/sharepoint/spo-team-site.json
  - src/rules/sharepoint/spo-communication-site.json
  - src/rules/sharepoint/spo-hub-site.json
  - src/data/segments/teams/entra-attributes.json
  - src/locales/en.json
  - src/locales/fr.json
autonomous: true
requirements: [TEAMS-01, TEAMS-02, TEAMS-03, TEAMS-04, SPO-01, SPO-02, SPO-03]

must_haves:
  truths:
    - "User can select 'Teams' in category navigation and build project-team (PRJ-), department-team (DEPT-) and service-team (SRV-) names with ≤30-char validation"
    - "User can build an Entra naming-policy-format name GRP_<GroupName>_<Department> with attribute-token dropdown segments and a blocked-words tip (TEAMS-04)"
    - "User can select 'SharePoint' and build Team-site (/teams/<url>, lowercase-hyphenated), Communication-site (/sites/<url>) and Hub-site (<Function> Hub) names"
    - "SPO-01 shows a tip that group-connected sites inherit the M365 group naming policy — no duplicated policy convention (D-08)"
    - "Every new locale key exists in BOTH en.json and fr.json (parity holds)"
    - "D-07: new directories src/rules/teams + src/rules/sharepoint with mirrored src/data/segments/teams catalog, kebab-case entra-id style"
    - "D-09: attribute-token segments ([Department][Company][Office]...) are dropdowns backed by teams/entra-attributes.json sample values"
    - "D-10: blocked words are a plain field tip only — no blocked-list enforcement, no new validation capability"
    - "D-12: Teams ≤30-char maxLength + SharePoint lowercase-hyphenated allowedPattern via convention-level validation; validation-rules.json untouched"
  artifacts:
    - path: "src/rules/teams/*.json"
      provides: "4 Teams conventions (project/department/service team + M365 group naming policy)"
    - path: "src/rules/sharepoint/*.json"
      provides: "3 SharePoint conventions (team site, communication site, hub site)"
    - path: "src/data/segments/teams/entra-attributes.json"
      provides: "Entra attribute-token dropdown library ([Department][Company][Office][StateOrProvince][CountryOrRegion][Title])"
    - path: "src/locales/en.json"
      provides: "EN keys: data.rule.<id>.name/.description x7, tipped-field labels/tips, new lib value descriptions"
    - path: "src/locales/fr.json"
      provides: "FR mirror of every new EN key"
  key_links:
    - from: "src/rules/**/*.json"
      to: "src/lib/data.ts ruleModules glob"
      via: "import.meta.glob('../rules/**/*.json') auto-discovery"
      pattern: "\"category\": \"(Teams|SharePoint)\""
    - from: "src/rules/teams/m365-group-naming-policy.json"
      to: "src/data/segments/teams/entra-attributes.json"
      via: "field.library reference 'teams/entra-attributes'"
      pattern: "\"library\": \"teams/entra-attributes\""
    - from: "src/rules/sharepoint/spo-team-site.json"
      to: "m365-group-naming-policy (Teams category)"
      via: "field tip referencing naming-policy inheritance (no second convention)"
      pattern: "naming policy"
---

<objective>
Create the Teams and SharePoint categories: 7 new JSON naming conventions (TEAMS-01..04, SPO-01..03) plus the Entra attribute-token segment library, fully wired for auto-discovery and bilingual (EN/FR) display.

Purpose: Deliverables 13a + 13b of Phase 13 — users can build governed Teams and SharePoint names without any code changes (categories appear via the existing JSON glob).
Output: 7 rule JSONs, 1 segment library, EN/FR locale keys; build/lint/test green.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-new-microsoft-365-categories-teams-sharepoint-power-platform/13-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/codebase/CONVENTIONS.md

**Environment caveat:** node_modules currently holds Windows-only native bindings — run `npm ci` once before `npm run build` / `npm run lint` if on Linux (STATE.md blocker note).

<interfaces>
<!-- Schema: src/types/Rule.ts (NamingConvention / NamingField / NamingBuilderConfig / NamingValidationConfig / NamingPatternOption). Key fields used below: -->
<!-- NamingValidationConfig: { maxLength?, allowedPattern?, forceLowercase?, removeCharacters? } -->
<!-- NamingField: { name, label?, type?: "text"|"dropdown", defaultValue?, library?, allowedValues?, examples?, tip?, allowCustomValue? } -->
<!-- NamingBuilderConfig: { separator?, defaultSegments?, availableSegments?, lockedSegments?, recommendedSegments?, fixedFirstSegments?, fixedLastSegments? } -->

<!-- Localization resolution (src/lib/segments.ts + src/hooks/useAppState.ts): -->
<!-- rule name/description:  tl(`data.rule.${id}.name|.description`, jsonValue) -->
<!-- field label/tip:        tl(`data.field.${FieldName}.label|.tip`, fallback) -->
<!-- library value desc:     tl(`data.lib.${field.library}.${valueCode}`, fallback) -->
<!-- Fallbacks mean missing keys don't crash — but EN/FR parity is a project constraint: ADD ALL KEYS to BOTH files. -->

<!-- Exemplar rule (shape to replicate): src/rules/groups/group-m365.json — inline allowedValues, examples[], builder with fixedFirstSegments, validation {maxLength, allowedPattern} -->
<!-- Segment library shape: { "name": string, "values": [{ "value": string, "description": string }] } (see src/data/segments/core/environment.json) -->
<!-- Reusable core libraries available: core/region, core/department, core/function, core/purpose, core/country -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Teams category — 4 conventions + entra-attributes segment library</name>
  <files>src/rules/teams/team-project.json, src/rules/teams/team-department.json, src/rules/teams/team-service.json, src/rules/teams/m365-group-naming-policy.json, src/data/segments/teams/entra-attributes.json, src/locales/en.json, src/locales/fr.json</files>
  <read_first>src/types/Rule.ts, src/rules/groups/group-m365.json, src/data/segments/core/environment.json, src/data/segments/core/region.json, .planning/research/SUMMARY.md (Microsoft Teams section), .planning/phases/13-new-microsoft-365-categories-teams-sharepoint-power-platform/13-CONTEXT.md</read_first>
  <action>
Create directory `src/rules/teams/` and `src/data/segments/teams/` (per D-07, mirroring `entra-id`/`entra` kebab-case style). Category value is `"Teams"` everywhere (short product name, per D-05).

**1. Segment library `src/data/segments/teams/entra-attributes.json`** (D-09 — attribute-token dropdowns backed by sample values; real values come from the tenant's directory):
```json
{
  "name": "Entra Attribute",
  "values": [
    { "value": "[Department]",       "description": "Department attribute from Entra ID" },
    { "value": "[Company]",          "description": "Company attribute from Entra ID" },
    { "value": "[Office]",           "description": "Office location attribute from Entra ID" },
    { "value": "[StateOrProvince]",  "description": "State or province attribute from Entra ID" },
    { "value": "[CountryOrRegion]",  "description": "Country or region attribute from Entra ID" },
    { "value": "[Title]",            "description": "Job title attribute from Entra ID" }
  ]
}
```
These six are exactly the attributes Entra's naming policy supports (research: `GRP_[GroupName]_[Department]` format, supported attributes `[Department][Company][Office][StateOrProvince][CountryOrRegion][Title]`).

**2–4. Three team conventions** — identical structure except prefix and description (TEAMS-01/02/03):

`team-project.json`: id `teams-project-team`, name `Project Team`.
`team-department.json`: id `teams-department-team`, name `Department Team`.
`team-service.json`: id `teams-service-team`, name `Service Team`.

Shared shape (shown for Project Team — copy for the other two with `PRJ-`→`DEPT-`/`SRV-` and adjusted description/examples):
```json
{
  "id": "teams-project-team",
  "category": "Teams",
  "name": "Project Team",
  "description": "Microsoft Teams project team. Team name is the M365 group display name — keep it <=30 characters (the Teams client truncates around 30-36), use '-' or '_' after the prefix, spaces inside the name are allowed.",
  "pattern": "[Prefix][Workload]-[Function]",
  "fields": [
    {
      "name": "Prefix",
      "type": "dropdown",
      "defaultValue": "PRJ",
      "allowedValues": ["PRJ"],
      "allowCustomValue": false,
      "tip": "Project teams use the PRJ- prefix."
    },
    {
      "name": "Workload",
      "type": "text",
      "defaultValue": "",
      "examples": ["Atlas", "Portal", "Migration"],
      "allowCustomValue": true
    },
    {
      "name": "Function",
      "library": "core/function",
      "defaultValue": "",
      "allowCustomValue": true
    }
  ],
  "examples": ["PRJ-Atlas-Finance"],
  "builder": {
    "separator": "-",
    "defaultSegments": ["Prefix", "Workload", "Function"],
    "availableSegments": [],
    "recommendedSegments": ["Function"],
    "lockedSegments": [],
    "fixedFirstSegments": ["Prefix"],
    "fixedLastSegments": []
  },
  "validation": {
    "maxLength": 30,
    "allowedPattern": "^[A-Za-z0-9 _-]+$"
  }
}
```
Notes: `maxLength: 30` implements the ≤30-char visibility guidance (TEAMS-01 success criterion, D-12); `allowedPattern` permits spaces inside the name (community guidance) while blocking emojis/specials; DEPT-/SRV- variants get matching `tip` strings ("Department teams use the DEPT- prefix." / "Service teams use the SRV- prefix.") and examples (`DEPT-HR-Operations`, `SRV-SecurityOps-Monitoring`).

**5. `src/rules/teams/m365-group-naming-policy.json`** (TEAMS-04, D-08 — THE single M365 group naming-policy convention):
```json
{
  "id": "m365-group-naming-policy",
  "category": "Teams",
  "name": "M365 Group Naming Policy",
  "description": "Entra ID group naming policy format applied to Microsoft 365 groups (and therefore Teams). Prefix and suffix strings are restricted at policy creation; blocked words (list up to 5000) are rejected automatically.",
  "pattern": "GRP_[GroupName]_[Department]",
  "fields": [
    {
      "name": "PolicyPrefix",
      "type": "text",
      "defaultValue": "GRP",
      "allowedValues": ["GRP"],
      "allowCustomValue": false
    },
    {
      "name": "GroupName",
      "type": "text",
      "defaultValue": "",
      "examples": ["Sales Reporting"],
      "allowCustomValue": true,
      "tip": "Avoid emojis. Blocked words configured in the Entra naming policy are rejected automatically."
    },
    {
      "name": "Department",
      "library": "teams/entra-attributes",
      "defaultValue": "[Department]",
      "allowCustomValue": true,
      "tip": "Supported Entra attributes: [Department] [Company] [Office] [StateOrProvince] [CountryOrRegion] [Title]. Real values resolve from your tenant's directory."
    }
  ],
  "examples": ["GRP_Sales Reporting_[Department]"],
  "builder": {
    "separator": "_",
    "defaultSegments": ["PolicyPrefix", "GroupName", "Department"],
    "availableSegments": [],
    "recommendedSegments": [],
    "lockedSegments": ["PolicyPrefix"],
    "fixedFirstSegments": ["PolicyPrefix"],
    "fixedLastSegments": []
  },
  "validation": {
    "maxLength": 64,
    "allowedPattern": "^[A-Za-z0-9 _\\[\\]-]+$"
  }
}
```
Blocked-words guidance is a plain field tip only (D-10 — NO blocked-list enforcement, no new validation capability). The `allowedPattern` explicitly allows `[` `]` so attribute-token examples validate.

**6. Locale keys** — add to BOTH `src/locales/en.json` and `src/locales/fr.json` under the flat `data.*` namespace (mirror how existing entries look, e.g. `data.rule.groups-m365.name`; keep FR translations natural French):
- `data.rule.teams-project-team.name` / `.description`, `data.rule.teams-department-team.name` / `.description`, `data.rule.teams-service-team.name` / `.description`, `data.rule.m365-group-naming-policy.name` / `.description` (EN values = the JSON name/description strings above)
- Tipped fields: `data.field.Prefix.label` already exists — do NOT duplicate existing field keys; only add NEW ones: `data.field.PolicyPrefix.label`, `data.field.GroupName.label`, `data.field.GroupName.tip`, `data.field.Workload.label`, plus `.tip` keys for each of the three prefix tips (keyed by field name `Prefix` — since all three conventions share the field name `Prefix` with different tips, instead key the tip per-convention is NOT possible with the current `data.field.<Name>.tip` scheme; therefore use ONE neutral shared tip: EN `data.field.Prefix.tip` = "Team type prefix: PRJ- (project), DEPT- (department) or SRV- (service)." — write the three per-convention nuances in the rule descriptions instead)
- `data.field.Department.tip` already exists for core Department — the entra-attributes Department field resolves `data.field.Department.tip`; OVERRIDE risk: adding a generic key would change existing Groups behavior. Therefore put the attribute-list guidance ONLY in this convention's field `tip` JSON string (fallback path) and additionally add `data.lib.teams/entra-attrs`… NO — correct approach: add `data.lib.teams/entra-attributes.[Department]` … `data.lib.teams/entra-attributes.[Title]` value-description keys (6 keys x2 languages) using the descriptions above, and leave `data.field.Department.*` untouched (existing key keeps working for core Department; the JSON `tip` serves as fallback for this convention).

FR translations: translate descriptions/tips faithfully (e.g. "Équipe de projet", "Politique de nommage des groupes M365", "Éviter les émojis. Les mots bloqués configurés dans la stratégie de nommage Entra sont rejetés automatiquement.").

Do NOT touch `validation-rules.json` (D-12: no new category keys there).
  </action>
  <verify>
    <automated>node -e "
const fs=require('fs');
for (const f of ['team-project','team-department','team-service','m365-group-naming-policy']) {
  const j=JSON.parse(fs.readFileSync('src/rules/teams/'+f+'.json','utf8'));
  if (j.category!=='Teams') throw new Error(f+': category');
}
const seg=JSON.parse(fs.readFileSync('src/data/segments/teams/entra-attributes.json','utf8'));
if (seg.values.length!==6) throw new Error('segment count');
const en=JSON.parse(fs.readFileSync('src/locales/en.json','utf8')), fr=JSON.parse(fs.readFileSync('src/locales/fr.json','utf8'));
for (const id of ['teams-project-team','teams-department-team','teams-service-team','m365-group-naming-policy']) {
  for (const k of ['name','description']) {
    if (!en.data['data.rule.'+id+'.'+k]) throw new Error('en missing '+id+'.'+k);
    if (!fr.data['data.rule.'+id+'.'+k]) throw new Error('fr missing '+id+'.'+k);
  }
}
console.log('TASK1 OK')" && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/rules/teams/` returns exactly: m365-group-naming-policy.json team-department.json team-project.json team-service.json
    - Each teams rule JSON parses AND contains `"category": "Teams"`
    - team-project/department/service each contain `"maxLength": 30` and prefixes PRJ/DEPT/SRV respectively in `allowedValues`
    - m365-group-naming-policy.json contains `"pattern": "GRP_[GroupName]_[Department]"` and `"separator": "_"`
    - entra-attributes.json contains all 6 attribute values including `[StateOrProvince]` and `[CountryOrRegion]`
    - en.json AND fr.json each contain `data.rule.m365-group-naming-policy.name`
    - `npm run build` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>4 Teams conventions + entra-attributes library created; EN/FR keys present; build/lint green</done>
</task>

<task type="auto">
  <name>task 2: SharePoint category — 3 conventions</name>
  <files>src/rules/sharepoint/spo-team-site.json, src/rules/sharepoint/spo-communication-site.json, src/rules/sharepoint/spo-hub-site.json, src/locales/en.json, src/locales/fr.json</files>
  <read_first>src/types/Rule.ts, src/rules/groups/group-m365.json, .planning/research/SUMMARY.md (SharePoint section), .planning/phases/13-new-microsoft-365-categories-teams-sharepoint-power-platform/13-CONTEXT.md</read_first>
  <action>
Create directory `src/rules/sharepoint/`. Category value is `"SharePoint"` (short name per D-05 — REQUIREMENTS' "SharePoint / OneDrive" wording is intentionally narrowed; OneDrive is deferred to Future Requirements).

**1. `spo-team-site.json`** (SPO-01 — group-connected Team site; D-08 inheritance tip):
```json
{
  "id": "spo-team-site",
  "category": "SharePoint",
  "name": "Team Site (Group-Connected)",
  "description": "Group-connected SharePoint Team site provisioned at /teams/<url>. The site name becomes the M365 group display name and inherits the Entra group naming policy.",
  "pattern": "[SiteName]",
  "fields": [
    {
      "name": "SiteName",
      "type": "text",
      "defaultValue": "",
      "examples": ["sales-eu", "hr-onboarding"],
      "allowCustomValue": true,
      "tip": "URL name: lowercase, hyphenated, no spaces. Group-connected sites inherit the M365 Group Naming Policy (see Teams category). Total file path stays under 400 chars — keep URLs short."
    }
  ],
  "examples": ["sales-eu"],
  "builder": {
    "separator": "-",
    "defaultSegments": ["SiteName"],
    "recommendedSegments": [],
    "lockedSegments": [],
    "fixedFirstSegments": [],
    "fixedLastSegments": []
  },
  "validation": {
    "forceLowercase": true,
    "allowedPattern": "^[a-z0-9-]+$"
  }
}
```

**2. `spo-communication-site.json`** (SPO-02 — `/sites/<url>`): same shape as spo-team-site with id `spo-communication-site`, name `Communication Site`, description mentioning provisioning at `/sites/<url>` and that the title can be renamed later independently of the URL, example `comms-finance-portal`, same `forceLowercase` + `^[a-z0-9-]+$` validation. Its SiteName tip should NOT repeat the group-policy inheritance line (that's SPO-01-only per D-08); instead: "URL name: lowercase, hyphenated, no spaces. Title can be renamed later independently of the URL."

**3. `spo-hub-site.json`** (SPO-03 — consistent hub naming, e.g. `<Function> Hub`):
```json
{
  "id": "spo-hub-site",
  "category": "SharePoint",
  "name": "Hub Site",
  "description": "SharePoint hub site. Microsoft recommends a consistent, discoverable naming convention for hub sites, e.g. '<Function> Hub'.",
  "pattern": "[Function] Hub",
  "fields": [
    {
      "name": "Function",
      "library": "core/function",
      "defaultValue": "",
      "allowCustomValue": true,
      "tip": "Use one consistent '<Function> Hub' formula across all hubs for discoverability."
    }
  ],
  "examples": ["Finance Hub"],
  "builder": {
    "separator": " ",
    "defaultSegments": ["Function", "Hub"],
    "availableSegments": [],
    "recommendedSegments": [],
    "lockedSegments": ["Hub"],
    "fixedFirstSegments": ["Function"],
    "fixedLastSegments": []
  },
  "validation": {
    "allowedPattern": "^[A-Za-z0-9 ]+$"
  }
}
```
For the trailing literal `Hub` segment: implement it as a field `{ "name": "Hub", "type": "text", "defaultValue": "Hub", "allowedValues": ["Hub"], "allowCustomValue": false }`.

**4. Locale keys** in BOTH en.json and fr.json (same mechanics as task 1):
- `data.rule.spo-team-site.name` / `.description`, `data.rule.spo-communication-site.name` / `.description`, `data.rule.spo-hub-site.name` / `.description`
- `data.field.SiteName.label` + `data.field.SiteName.tip` (use the SPO-01 inheritance tip text as the canonical entry — the communication-site variant lives only in its JSON fallback)
- FR translations faithful (e.g. "Site d'équipe (connecté à un groupe)", "le nom du site hérite de la stratégie de nommage des groupes M365").

Do NOT create a second naming-policy convention here (D-08: one policy convention, in Teams).
  </action>
  <verify>
    <automated>node -e "
const fs=require('fs');
for (const f of ['spo-team-site','spo-communication-site','spo-hub-site']) {
  const j=JSON.parse(fs.readFileSync('src/rules/sharepoint/'+f+'.json','utf8'));
  if (j.category!=='SharePoint') throw new Error(f+': category');
}
if (!JSON.parse(fs.readFileSync('src/rules/sharepoint/spo-team-site.json','utf8')).validation.forceLowercase) throw new Error('forceLowercase');
const en=JSON.parse(fs.readFileSync('src/locales/en.json','utf8')), fr=JSON.parse(fs.readFileSync('src/locales/fr.json','utf8'));
for (const id of ['spo-team-site','spo-communication-site','spo-hub-site']) {
  if (!en.data['data.rule.'+id+'.name'] || !fr.data['data.rule.'+id+'.description']) throw new Error('locale '+id);
}
console.log('TASK2 OK')" && npm run build && npm run lint && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/rules/sharepoint/` returns exactly: spo-communication-site.json spo-hub-site.json spo-team-site.json
    - All three contain `"category": "SharePoint"`; spo-team-site and spo-communication-site contain `"forceLowercase": true` and `"^[a-z0-9-]+$"`
    - spo-team-site contains the string "naming policy" (inheritance tip, D-08); grep confirms exactly ONE file in src/rules/ matches "naming policy" convention duplication — `grep -rl "M365 Group Naming Policy" src/rules/` returns ONLY src/rules/teams/m365-group-naming-policy.json
    - spo-hub-site contains `"pattern": "[Function] Hub"` and locked `Hub` segment
    - en.json and fr.json contain `data.rule.spo-team-site.name` and `data.field.SiteName.tip`
    - `npm run build`, `npm run lint`, `npm test` all exit 0
  </acceptance_criteria>
  <done>3 SharePoint conventions created with URL validation + inheritance tip; EN/FR parity; triple gate green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON config → regex engine | `validation.allowedPattern` strings from rule JSONs are compiled to RegExp by `validateName` |
| Locale files → DOM | New EN/FR strings render through React text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering/DoS | `allowedPattern` regexes added by this plan | mitigate | Only simple anchored character-class patterns (`^[A-Za-z0-9 _-]+$`, `^[a-z0-9-]+$`) — no nested quantifiers, no alternation overlap that could cause ReDoS |
| T-13-01-02 | Information Disclosure | Attribute-token sample values in entra-attributes.json | accept | Sample values are generic placeholders, no real tenant data |
| T-13-01-03 | Elevation | New JSON cannot execute code (loaded as data via import.meta.glob) | accept | Data-only files; schema validated by TypeScript types at build |
</threat_model>

<verification>
- `npm run build && npm run lint && npm test -- --run` all exit 0
- `ls src/rules/teams src/rules/sharepoint` shows 4 + 3 JSON files, all parseable
- Node parity check: every new `data.rule.*` key present in both en.json and fr.json
</verification>

<success_criteria>
- 7 conventions discoverable at build time (glob picks up new directories with zero registration code)
- Teams: PRJ-/DEPT-/SRV- names capped at 30 chars; GRP_ naming-policy format builds with attribute-token dropdowns and blocked-words tip
- SharePoint: lowercase-hyphenated URL validation active; hub-site `<Function> Hub` formula; single naming-policy convention (no duplication)
- EN/FR parity maintained; triple gate green
</success_criteria>

<output>
After completion, create `.planning/phases/13-new-microsoft-365-categories-teams-sharepoint-power-platform/13-01-SUMMARY.md`
</output>
