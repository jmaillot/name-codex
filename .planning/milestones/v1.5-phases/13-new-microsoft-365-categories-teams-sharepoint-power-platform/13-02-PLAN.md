---
phase: 13-new-microsoft-365-categories-teams-sharepoint-power-platform
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/rules/power-platform/pp-environment.json
  - src/rules/purview/purview-sensitivity-label.json
  - src/rules/purview/purview-retention-label.json
  - src/rules/purview/purview-retention-policy.json
  - src/rules/purview/purview-dlp-policy.json
  - src/data/segments/power-platform/lifecycle.json
  - src/data/segments/purview/classification.json
  - src/locales/en.json
  - src/locales/fr.json
autonomous: true
requirements: [PP-01, PURV-01, PURV-02, PURV-03, PURV-04]

must_haves:
  truths:
    - "User can select 'Power Platform' and build an environment name per ALM strategy <lifecycle>-<region>-<bu>-<purpose> with sandbox-vs-production type noted (PP-01)"
    - "User can select 'Purview' and build sensitivity-label names from the classification taxonomy with sublabel support (PURV-01)"
    - "User can build retention-label names with immutable-after-save and no-double-quotes tips (PURV-02)"
    - "User can build retention-policy names with the 7-day application lead-time tip (PURV-03)"
    - "User can build DLP-policy names with the audit-persistence tip (PURV-04)"
    - "D-12: PP environment enforced via convention-level validation (<lifecycle>-<region>-<bu>-<purpose>, forceLowercase); validation-rules.json untouched"
    - "D-14: PURV-01 is ONE convention JSON file with two pattern variants in patterns[] (parent label + sublabel) keeping the 12-convention count intact"
    - "Every new locale key exists in BOTH en.json and fr.json"
  artifacts:
    - path: "src/rules/power-platform/pp-environment.json"
      provides: "Power Platform environment convention per ALM strategy"
    - path: "src/rules/purview/*.json"
      provides: "4 Purview conventions (sensitivity label w/ patterns[] variants, retention label, retention policy, DLP policy)"
    - path: "src/data/segments/power-platform/lifecycle.json"
      provides: "New lifecycle value set (dev/test/prod/sandbox) — genuinely new values per D-11"
    - path: "src/data/segments/purview/classification.json"
      provides: "Classification taxonomy: Personal/Public/General/Confidential/Highly Confidential"
    - path: "src/locales/en.json + src/locales/fr.json"
      provides: "EN/FR keys for 5 rules, tipped fields, and new library value descriptions"
  key_links:
    - from: "src/rules/power-platform/pp-environment.json"
      to: "src/data/segments/power-platform/lifecycle.json + core libraries"
      via: "field.library references"
      pattern: "\"library\": \"power-platform/lifecycle\""
    - from: "src/rules/purview/purview-sensitivity-label.json"
      to: "src/data/segments/purview/classification.json"
      via: "field.library reference + patterns[] two variants"
      pattern: "\"patterns\""
    - from: "src/rules/purview/*"
      to: "InfoIcon tooltip mechanism"
      via: "field.tip strings carrying Purview gotchas (D-13)"
      pattern: "\"tip\":"
---

<objective>
Create the Power Platform and Purview categories: 5 new JSON naming conventions (PP-01, PURV-01..04) plus two new segment libraries (lifecycle, classification), with all Microsoft-documented gotchas surfaced as bilingual field tips.

Purpose: Deliverables 13c + 13d of Phase 13 — users can build governed Power Platform environment and Purview compliance-object names.
Output: 5 rule JSONs, 2 segment libraries, EN/FR locale keys; build/lint/test green.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-new-microsoft-365-categories-teams-sharepoint-power-platform/13-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/codebase/CONVENTIONS.md

**Environment caveat:** run `npm ci` once before build/lint on Linux (Windows-only native bindings in node_modules).

<interfaces>
<!-- Schema: src/types/Rule.ts. NamingPatternOption supports multiple pattern variants on ONE convention: -->
<!-- { id, name, description?, pattern, defaultSegments?, examples?, fixedValues?, fields? } — set via convention.patterns[] -->
<!-- NamingValidationConfig: { maxLength?, allowedPattern?, forceLowercase?, removeCharacters? } -->
<!-- removeCharacters is ideal for Purview's no-double-quotes rule (PURV-02). -->

<!-- Localization resolution: data.rule.<id>.name|.description · data.field.<FieldName>.label|.tip · data.lib.<library>.<valueCode> — add ALL new keys to BOTH en.json and fr.json -->

<!-- Exemplar multi-segment rule: src/rules/groups/group-m365.json; segment library shape: { name, values:[{value,description}] } -->
<!-- Reusable core libraries (D-11): core/environment has PROD/PREPROD/UAT/TEST/DEV/LAB/PILOT — PP lifecycle needs its OWN file because sandbox-type semantics differ (D-11: new files only for genuinely new value sets) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Power Platform category — environment convention + lifecycle library</name>
  <files>src/rules/power-platform/pp-environment.json, src/data/segments/power-platform/lifecycle.json, src/locales/en.json, src/locales/fr.json</files>
  <read_first>src/types/Rule.ts, src/rules/groups/group-m365.json, src/data/segments/core/environment.json, src/data/segments/core/region.json, .planning/research/SUMMARY.md (Power Platform section)</read_first>
  <action>
Create directories `src/rules/power-platform/` and `src/data/segments/power-platform/`. Category value `"Power Platform"`.

**1. `src/data/segments/power-platform/lifecycle.json`** (new value set per D-11 — lowercase ALM lifecycle tokens distinct from core Environment):
```json
{
  "name": "Lifecycle",
  "values": [
    { "value": "dev",  "description": "Developer environment" },
    { "value": "test", "description": "Test environment" },
    { "value": "sbx",  "description": "Sandbox environment" },
    { "value": "uat",  "description": "User acceptance testing environment" },
    { "value": "prod", "description": "Production environment" }
  ]
}
```

**2. `src/rules/power-platform/pp-environment.json`** (PP-01):
```json
{
  "id": "pp-environment",
  "category": "Power Platform",
  "name": "Power Platform Environment",
  "description": "Power Platform environment named per ALM environment strategy: <lifecycle>-<region>-<business unit>-<purpose>. Maintain at minimum Dev/Test/Prod. When creating an environment, choose its type explicitly (Sandbox vs Production) — the lifecycle token should match that type. Rename the default environment ('{tenant} (default)') to a descriptive name.",
  "pattern": "[Lifecycle]-[Region]-[BusinessUnit]-[Purpose]",
  "fields": [
    {
      "name": "Lifecycle",
      "library": "power-platform/lifecycle",
      "defaultValue": "dev",
      "allowCustomValue": true,
      "tip": "Match the lifecycle token to the environment TYPE chosen in the Power Platform admin center: Sandbox types (Dev/Test/Sandbox) vs Production."
    },
    {
      "name": "Region",
      "library": "core/region",
      "defaultValue": "",
      "allowCustomValue": true
    },
    {
      "name": "BusinessUnit",
      "library": "core/department",
      "defaultValue": "",
      "allowCustomValue": true,
      "examples": ["IT", "FIN", "OPS"]
    },
    {
      "name": "Purpose",
      "library": "core/purpose",
      "defaultValue": "",
      "allowCustomValue": true
    }
  ],
  "examples": ["dev-westeurope-it-sandbox", "prod-eastus-fin-reporting"],
  "builder": {
    "separator": "-",
    "defaultSegments": ["Lifecycle", "Region", "BusinessUnit", "Purpose"],
    "availableSegments": [],
    "recommendedSegments": ["BusinessUnit"],
    "lockedSegments": [],
    "fixedFirstSegments": ["Lifecycle"],
    "fixedLastSegments": []
  },
  "validation": {
    "forceLowercase": true,
    "allowedPattern": "^[a-z0-9-]+$"
  }
}
```
Reuse core libraries wherever values fit (Region/Department/Purpose) — only lifecycle is a new file (D-11).

**3. Locale keys** in BOTH en.json and fr.json:
- `data.rule.pp-environment.name` / `.description`
- `data.lib.power-platform/lifecycle.dev|test|sbx|uat|prod` (5 x2 languages, descriptions above)
- `data.field.Lifecycle.label`, `data.field.Lifecycle.tip`, `data.field.BusinessUnit.label`
- FR faithful (e.g. "Environnement Power Platform", "Stratégie ALM : maintenez au minimum Dév/Test/Prod.")
  </action>
  <verify>
    <automated>node -e "
const fs=require('fs');
const j=JSON.parse(fs.readFileSync('src/rules/power-platform/pp-environment.json','utf8'));
if (j.category!=='Power Platform' || j.pattern!=='[Lifecycle]-[Region]-[BusinessUnit]-[Purpose]') throw new Error('shape');
if (!j.validation.forceLowercase) throw new Error('lowercase');
const seg=JSON.parse(fs.readFileSync('src/data/segments/power-platform/lifecycle.json','utf8'));
if (seg.values.length!==5) throw new Error('lifecycle count');
const en=JSON.parse(fs.readFileSync('src/locales/en.json','utf8')), fr=JSON.parse(fs.readFileSync('src/locales/fr.json','utf8'));
if (!en.data['data.rule.pp-environment.name'] || !fr.data['data.rule.pp-environment.description']) throw new Error('locale');
console.log('TASK1 OK')" && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - pp-environment.json contains `"category": "Power Platform"` and `"pattern": "[Lifecycle]-[Region]-[BusinessUnit]-[Purpose]"` and `"forceLowercase": true`
    - Lifecycle JSON contains exactly dev/test/sbx/uat/prod
    - en.json AND fr.json contain `data.rule.pp-environment.name`, `data.lib.power-platform/lifecycle.sbx`, `data.field.Lifecycle.tip`
    - `npm run build` and `npm run lint` exit 0
  </acceptance_criteria>
  <done>PP-01 delivered: ALM-pattern environment names with sandbox-vs-production guidance, EN/FR parity</done>
</task>

<task type="auto">
  <name>task 2: Purview sensitivity label — one convention, two pattern variants</name>
  <files>src/rules/purview/purview-sensitivity-label.json, src/data/segments/purview/classification.json, src/locales/en.json, src/locales/fr.json</files>
  <read_first>src/types/Rule.ts (NamingPatternOption), src/rules/groups/group-m365.json, .planning/research/SUMMARY.md (Purview section), 13-CONTEXT.md D-14</read_first>
  <action>
Create directories `src/rules/purview/` and `src/data/segments/purview/`. Category value `"Purview"`.

**1. `src/data/segments/purview/classification.json`**:
```json
{
  "name": "Classification",
  "values": [
    { "value": "Personal",            "description": "Minimal personal data" },
    { "value": "Public",              "description": "Approved for public release" },
    { "value": "General",             "description": "Default level, not public" },
    { "value": "Confidential",        "description": "Sensitive business data" },
    { "value": "Highly Confidential", "description": "Most sensitive business data" }
  ]
}
```

**2. `src/rules/purview/purview-sensitivity-label.json`** (PURV-01 — ONE convention file with TWO variants in `patterns[]` keeps the roadmap's 12-convention count intact, per D-14):
```json
{
  "id": "purview-sensitivity-label",
  "category": "Purview",
  "name": "Sensitivity Label",
  "description": "Microsoft Purview sensitivity label from the classification taxonomy, with optional sublabel variant.",
  "pattern": "[Classification]",
  "patterns": [
    {
      "id": "parent-label",
      "name": "Parent Label",
      "description": "Top-level label from the classification taxonomy",
      "pattern": "[Classification]",
      "defaultSegments": ["Classification"],
      "examples": ["Confidential"]
    },
    {
      "id": "sublabel",
      "name": "Sublabel",
      "description": "Sublabel nested under a parent label",
      "pattern": "[Classification]-[Sublabel]",
      "defaultSegments": ["Classification", "Sublabel"],
      "examples": ["Confidential-Payroll"]
    }
  ],
  "fields": [
    {
      "name": "Classification",
      "library": "purview/classification",
      "defaultValue": "General",
      "allowCustomValue": false,
      "tip": "Use your tenant's published taxonomy order: Personal > Public > General > Confidential > Highly Confidential."
    },
    {
      "name": "Sublabel",
      "type": "text",
      "defaultValue": "",
      "examples": ["Payroll", "M&A"],
      "allowCustomValue": true,
      "tip": "Sublabels inherit the parent label's settings; keep names short and stable."
    }
  ],
  "examples": ["General", "Confidential-Payroll"],
  "builder": {
    "separator": "-",
    "defaultSegments": ["Classification"],
    "availableSegments": ["Sublabel"],
    "recommendedSegments": [],
    "lockedSegments": [],
    "fixedFirstSegments": ["Classification"],
    "fixedLastSegments": []
  },
  "validation": {
    "removeCharacters": ["\""],
    "allowedPattern": "^[A-Za-z0-9 &/-]+$"
  }
}
```
The `removeCharacters: ["\""]` proactively enforces Purview's no-double-quotes constraint at generation time.

**3. Locale keys** BOTH languages:
- `data.rule.purview-sensitivity-label.name` / `.description`
- `data.lib.purview/classification.Personal|Public|General|Confidential|Highly Confidential` (5 x2)
- `data.field.Classification.label` / `.tip`, `data.field.Sublabel.label` / `.tip`
- FR faithful ("Étiquette de confidentialité", " Hautement confidentiel" etc.)
  </action>
  <verify>
    <automated>node -e "
const fs=require('fs');
const j=JSON.parse(fs.readFileSync('src/rules/purview/purview-sensitivity-label.json','utf8'));
if (j.category!=='Purview') throw new Error('category');
if (!Array.isArray(j.patterns) || j.patterns.length!==2) throw new Error('patterns[] variants');
const ids=j.patterns.map(p=>p.id);
if (!(ids.includes('parent-label') && ids.includes('sublabel'))) throw new Error('variant ids');
const seg=JSON.parse(fs.readFileSync('src/data/segments/purview/classification.json','utf8'));
if (seg.values.length!==5) throw new Error('taxonomy count');
console.log('TASK2 OK')" && npm run build && npm run lint && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - purview-sensitivity-label.json contains `"patterns"` array with EXACTLY 2 entries (parent-label, sublabel) and top-level `"pattern"` still present
    - classification.json contains all 5 taxonomy values including "Highly Confidential"
    - Validation contains `removeCharacters` including a double-quote character
    - en.json and fr.json contain `data.lib.purview/classification.Highly Confidential`
    - `npm run build`, `npm run lint`, `npm test` exit 0
  </acceptance_criteria>
  <done>PURV-01 delivered as single convention with parent/sublabel variants per D-14</done>
</task>

<task type="auto">
  <name>task 3: Purview retention labels/policies + DLP policy with gotcha tips</name>
  <files>src/rules/purview/purview-retention-label.json, src/rules/purview/purview-retention-policy.json, src/rules/purview/purview-dlp-policy.json, src/locales/en.json, src/locales/fr.json</files>
  <read_first>src/types/Rule.ts, src/rules/groups/group-m365.json, .planning/research/SUMMARY.md (Purview section), 13-CONTEXT.md D-13</read_first>
  <action>
Three more conventions in `src/rules/purview/`. All carry their Microsoft-documented gotchas as EN+FR tip strings via the existing InfoIcon tooltip mechanism (D-13).

**1. `purview-retention-label.json`** (PURV-02):
```json
{
  "id": "purview-retention-label",
  "category": "Purview",
  "name": "Retention Label",
  "description": "Microsoft Purview retention label. The name CANNOT be changed after save — validate the name carefully before creating it.",
  "pattern": "[LabelName]",
  "fields": [
    {
      "name": "LabelName",
      "type": "text",
      "defaultValue": "",
      "examples": ["Legal Hold 7Y", "Finance Records 10Y"],
      "allowCustomValue": true,
      "tip": "Immutable after save: the name cannot be changed later. Double quotes (\") are not supported. Label policies can take up to 7 days to apply — get the name right first time."
    }
  ],
  "examples": ["Legal Hold 7Y"],
  "builder": {
    "separator": " ",
    "defaultSegments": ["LabelName"],
    "recommendedSegments": [],
    "lockedSegments": [],
    "fixedFirstSegments": [],
    "fixedLastSegments": []
  },
  "validation": {
    "removeCharacters": ["\""],
    "allowedPattern": "^[A-Za-z0-9 &/_()-]+$"
  }
}
```

**2. `purview-retention-policy.json`** (PURV-03): same shape; id `purview-retention-policy`, name `Retention Policy`, description noting policies apply org-wide or to locations; example `Finance Retention EU`; PolicyName tip: "Policy names apply after up to a 7-day lead time and cannot be renamed casually — plan names before creation." Same validation block as the retention label.

**3. `purview-dlp-policy.json`** (PURV-04): id `purview-dlp-policy`, name `DLP Policy`; description: renamable, BUT old names persist forever in audit and alert records; PolicyName tip: "Old names persist permanently in audit/alert records even after renaming — choose audit-friendly names." Example `EU-PII-Protection`; same validation block.

**4. Locale keys** BOTH languages for each: `data.rule.<id>.name` / `.description` (6 keys x3 conventions... i.e. 2 keys x3 conventions), plus `data.field.LabelName.label` / `.tip`, `data.field.PolicyName.label` / `.tips`— NOTE: both retention-policy and dlp-policy use field name `PolicyName`; the `data.field.PolicyName.tip` key scheme holds ONE shared tip. Resolution (same approach as plan 13-01 Prefix): put the SHARED neutral part in the locale key — EN `data.field.PolicyName.tip`: "Retention policies take up to 7 days to apply; DLP old names persist in audit records even after rename." — and keep the per-convention precise wording in each rule's JSON `tip` fallback. Add `data.field.PolicyName.label`.
FR translations faithful ("étiquette de rétention", "immuable après enregistrement", "délai d'application de 7 jours", "les anciens noms persistent dans les enregistrements d'audit").
  </action>
  <verify>
    <automated>node -e "
const fs=require('fs');
for (const f of ['purview-retention-label','purview-retention-policy','purview-dlp-policy']) {
  const j=JSON.parse(fs.readFileSync('src/rules/purview/'+f+'.json','utf8'));
  if (j.category!=='Purview') throw new Error(f);
  const tips=JSON.stringify(j.fields);
  if (!tips.includes('tip')) throw new Error(f+': missing tip');
}
const rl=JSON.parse(fs.readFileSync('src/rules/purview/purview-retention-label.json','utf8'));
if (!rl.validation.removeCharacters.includes('\"')) throw new Error('no-double-quote guard');
const en=JSON.parse(fs.readFileSync('src/locales/en.json','utf8')), fr=JSON.parse(fs.readFileSync('src/locales/fr.json','utf8'));
for (const id of ['purview-retention-label','purview-retention-policy','purview-dlp-policy']) {
  if (!en.data['data.rule.'+id+'.name'] || !fr.data['data.rule.'+id+'.description']) throw new Error('locale '+id);
}
console.log('TASK3 OK')" && npm run build && npm run lint && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/rules/purview/` returns exactly 5 files (incl. task 2's sensitivity label)
    - purview-retention-label.json contains the strings "cannot be changed" (immutability) and a double-quote entry in removeCharacters
    - purview-retention-policy.json contains "7-day" or "7 days" (lead-time tip); purview-dlp-policy.json contains "audit" (persistence tip)
    - en.json and fr.json contain data.rule keys for all three ids
    - `npm run build`, `npm run lint`, `npm test` exit 0
  </acceptance_criteria>
  <done>PURV-02..04 delivered with immutable-after-save, 7-day lead-time, and audit-persistence tips in EN+FR (D-13)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON config → regex engine | New `allowedPattern` strings compiled by validateName |
| Locale files → DOM | New EN/FR strings render as React text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | DoS/ReDoS | New allowedPattern regexes | mitigate | Simple anchored character classes only (`^[a-z0-9-]+$`, `^[A-Za-z0-9 &/_()-]+$`) — bounded, no nested quantifiers |
| T-13-02-02 | Repudiation | Purview governance names carry compliance semantics | accept | Tool generates names only; actual label/policy creation stays in the Purview admin center |
| T-13-02-03 | Tampering | removeCharacters strips quotes at generation | accept | Defense-in-depth UX nicety; authoritative validation remains in Microsoft Purview itself |
</threat_model>

<verification>
- `npm run build && npm run lint && npm test -- --run` all exit 0
- `ls src/rules/power-platform src/rules/purview` shows 1 + 5 parseable JSON files
- Node parity check: all new data.rule.* / data.lib.* keys present in BOTH en.json and fr.json
</verification>

<success_criteria>
- PP-01: `<lifecycle>-<region>-<bu>-<purpose>` builds with lowercase enforcement and sandbox-vs-production tip
- PURV-01..04: all four conventions present; sensitivity label uses patterns[] two-variant structure (12-convention count preserved)
- Purview gotchas (immutable-after-save, no double quotes, 7-day lead time, audit persistence) surface as InfoIcon tips in EN and FR
- EN/FR parity maintained
</success_criteria>

<output>
After completion, create `.planning/phases/13-new-microsoft-365-categories-teams-sharepoint-power-platform/13-02-SUMMARY.md`
</output>
