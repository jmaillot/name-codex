---
phase: 13-new-microsoft-365-categories-teams-sharepoint-power-platform
plan: 04
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - src/types/Rule.ts
  - src/lib/validation.ts
  - src/lib/validation.test.ts
  - src/locales/en.json
  - src/locales/fr.json
  - src/rules/purview/purview-sensitivity-label.json
autonomous: true
requirements: [UAT-13-07]

must_haves:
  truths:
    - "User cannot enter spaces in the Purview Sensitivity Label Sublabel field — a validation row flags it while Classification values like 'Highly Confidential' remain valid (spaces still allowed there)"
    - "Per-field character restriction is generic: any NamingField may declare allowedPattern, enforced as an extra validation row in validateName"
    - "Every new locale key exists in BOTH en.json and fr.json (parity holds)"
    - "No behavior change for conventions/fields that do not declare a field-level allowedPattern (backwards compatible)"
  artifacts:
    - path: "src/types/Rule.ts"
      provides: "allowedPattern?: string added to NamingField"
    - path: "src/lib/validation.ts"
      provides: "validateName emits one localized validation row per segment whose matching field declares allowedPattern"
    - path: "src/rules/purview/purview-sensitivity-label.json"
      provides: "Sublabel field with \"allowedPattern\": \"^[A-Za-z0-9&/-]+$\" (no spaces) + updated no-spaces tip"
    - path: "src/locales/en.json"
      provides: "EN key ui.valFieldPattern + updated Sublabel tip"
    - path: "src/locales/fr.json"
      provides: "FR mirror of every new/changed EN key"
  key_links:
    - from: "src/rules/purview/purview-sensitivity-label.json"
      to: "src/lib/validation.ts validateName"
      via: "field-level allowedPattern consumed by per-segment validation"
      pattern: "\"allowedPattern\": \"\\^\\[A-Za-z0-9&/-\\]\\+\\$\""

---

<objective>
Close UAT gap UAT-13-07 (minor): Purview sensitivity label sublabel names should contain no spaces. Add generic per-field allowedPattern validation and apply it to the Sublabel field of purview-sensitivity-label.
</objective>

<tasks>

<task type="auto" name="Task 1: Per-field allowedPattern validation">
Read first: src/lib/validation.ts (whole file), src/types/Rule.ts, src/lib/validation.test.ts, src/hooks/useAppState.ts (segment value assembly ~line 240-260).

1. In `src/types/Rule.ts`, add `allowedPattern?: string;` to the `NamingField` interface (NOT NamingValidationConfig — that already has one).
2. In `validateName` (src/lib/validation.ts): after the four existing rows, iterate `segments`; for each segment whose `sourceName` matches a `convention.fields[]` entry declaring `allowedPattern`, push a row:
   - label key `ui.valFieldPattern` with interpolation `{ name }` → EN: "Segment {name} respects its allowed characters", FR: « Le segment {name} respecte ses caractères autorisés » (verify exact i18n interpolation helper used by neighboring rows — reuse `tl(...)` identically).
   - valid: `new RegExp(field.allowedPattern).test(segment.value) || segment.value.length === 0` (empty-value bypass mirrors the existing allowedPattern row).
   - Skip segments whose sourceName matches NO field or whose field lacks allowedPattern — zero impact elsewhere.
3. Unit tests in validation.test.ts: convention with a text field carrying `allowedPattern: "^[^ ]+$"` — value with space fails the new row, value without passes, other rows unaffected; convention without field patterns produces identical results to before.
4. Do NOT touch normalizeName/removeCharacters — stripping is out of scope; this is validation feedback only.
Gate: npm test -- --run green.
Commit: `feat(13-04): per-field allowedPattern validation`
</task>

<task type="auto" name="Task 2: Apply to Purview Sublabel + bilingual tips">
1. `src/rules/purview/purview-sensitivity-label.json` — Sublabel field: add `"allowedPattern": "^[A-Za-z0-9&/-]+$"` (no space in the class). Keep everything else intact (caseMode preserve stays at validation level).
2. Update the Sublabel `tip` so it states sublabel names contain no spaces (e.g. EN: "...keep names short, stable, and space-free." / FR mirror). Update the corresponding `data.field.Sublabel.tip` keys in BOTH src/locales/en.json and src/locales/fr.json if the tip is localized there rather than inline — check where the current tip string lives first and edit in place.
3. Programmatic parity gate: node script diffing sorted key sets of en.json vs fr.json must report equal counts.
Gate: npm run build ✓ · npm run lint ✓ · npm test -- --run green.
Commit: `feat(13-04): space-free Purview sublabels`
</task>

<task type="checkpoint:human-verify" name="Task 3: Re-verify UAT test 7 scenario">
Ask the user to re-run the original failing flow: Purview → Sensitivity Label → Sublabel variant → type "Pay Roll" → validation shows the failing segment row; "Pay-Roll" passes; parent-label "Highly Confidential" still validates fine. On approval, write `.planning/phases/13-new-microsoft-365-categories-teams-sharepoint-power-platform/13-04-SUMMARY.md` documenting the gap closure (gap source: 13-UAT.md test 7, severity minor) and commit it: `docs(13-04): gap closure summary`.
</task>

</tasks>
