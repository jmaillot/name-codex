---
phase: 14-category-expansions-azure-caf-resources-intune
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/rules/azure/azure-storage-account.json
  - src/rules/azure/azure-key-vault.json
  - src/rules/azure/azure-network-security-group.json
  - src/rules/azure/azure-public-ip.json
  - src/data/segments/azure/caf-workloads.json
  - src/locales/en.json
  - src/locales/fr.json
autonomous: true
requirements: [AZURE-01, AZURE-02, AZURE-03, AZURE-04]
must_haves:
  truths:
    - "User selects the Azure category in navigation and sees 4 new conventions: Storage Account, Key Vault, Network Security Group, Public IP (backed by the dedicated CAF workload library per D-12)"
    - "User assembles a storage-account name that renders lowercase-alphanumeric with NO hyphens (e.g. stappprod001), capped at 24 chars (D-01, D-02, D-03, D-04)"
    - "User assembles a key-vault name capped at 24 chars whose generated name must start with a letter and end with a letter or number (D-09, D-10)"
    - "User assembles NSG (nsg-<workload>-<instance>) and public-IP (pip-<workload>-<env>-<region>-<instance>) names per CAF (D-11)"
    - "Every new EN locale key has an FR mirror (parity count equal)"
  artifacts:
    - path: "src/rules/azure/azure-storage-account.json"
      provides: "AZURE-01 storage account convention"
      contains: "\"pattern\": \"st[Workload][Environment][Instance]\""
    - path: "src/rules/azure/azure-key-vault.json"
      provides: "AZURE-02 key vault convention"
      contains: "\"maxLength\": 24"
    - path: "src/rules/azure/azure-network-security-group.json"
      provides: "AZURE-03 NSG convention"
      contains: "\"pattern\": \"nsg-[Workload]-[Instance]\""
    - path: "src/rules/azure/azure-public-ip.json"
      provides: "AZURE-04 public IP convention"
      contains: "\"pattern\": \"pip-[Workload]-[Environment]-[Region]-[Instance]\""
    - path: "src/data/segments/azure/caf-workloads.json"
      provides: "CAF workload abbreviation library"
      contains: "\"value\": \"st\""
  key_links:
    - from: "src/rules/azure/azure-storage-account.json"
      to: "src/data/validation-rules.json"
      via: "category/name key 'Azure' > 'Storage Account' shadows convention.validation (validation.ts:31)"
      pattern: "Storage Account"
    - from: "src/rules/azure/*.json"
      to: "src/data/segments/azure/caf-workloads.json"
      via: "field.library reference"
      pattern: "azure/caf-workloads"
    - from: "src/locales/en.json"
      to: "src/locales/fr.json"
      via: "flat key parity"
      pattern: "data\\.rule\\.azure-(storage-account|key-vault|network-security-group|public-ip)"
---

<objective>
Add the 4 CAF-standard Azure conventions (storage account, key vault, NSG, public IP) as pure data-driven JSON to the existing `src/rules/azure/` directory, backed by a new CAF workload-abbreviation segment library, fully bilingual EN/FR — zero TypeScript changes (per QUAL-01, D-01..D-12).

Purpose: Deliverable 14a of the roadmap — users can build governed Azure resource names including the deliberate no-hyphen/lowercase storage-account outlier that exercises `forceLowercase` + restrictive `allowedPattern`.

Output: 5 new JSON files + extended `en.json`/`fr.json`; auto-discovered by the existing `import.meta.glob` (no registration).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-category-expansions-azure-caf-resources-intune/14-CONTEXT.md
@.planning/phases/14-category-expansions-azure-caf-resources-intune/14-PATTERNS.md
@src/rules/azure/azure-resource-group.json
@src/rules/purview/purview-sensitivity-label.json
@src/data/validation-rules.json

<interfaces>
<!-- Schema knobs available (src/types/Rule.ts) — NO code changes needed: -->
NamingValidationConfig: { maxLength?, allowedPattern?, forceLowercase?, removeCharacters?: string[], caseMode?: "upper"|"lower"|"preserve" }
NamingBuilderConfig:   { separator?, defaultSegments[], availableSegments[], recommendedSegments[], lockedSegments[], fixedFirstSegments[], fixedLastSegments[] }
NamingField:           { name, label?, type?, library?, defaultValue?, allowedValues?, allowedValuesByField?, allowCustomValue?, allowedPattern?, tip?, examples?, generator?, customOnly?, placeholder? }

<!-- CRITICAL precedence rule (src/lib/validation.ts:31,45): -->
normalizeName/validateName resolve the validation rule as:
  validationRules?.[convention.category]?.[convention.name]  ← WINS if present
  ?? convention.validation
  ?? validationRules.default
`validation-rules.json` ALREADY contains `Azure > "Storage Account"`:
  { "maxLength": 24, "allowedPattern": "^[a-z0-9]+$", "forceLowercase": true, "removeCharacters": ["-","_","."] }
This block SHADOWS whatever azure-storage-account.json declares. Do NOT edit validation-rules.json (13-D-12 carried forward).

<!-- Separator resolution (src/hooks/useAppState.ts:227): -->
const separator = selectedConvention.builder?.separator ?? "-";
An OMITTED separator defaults to "-". For direct concatenation you MUST set "separator": "" (empty string passes the ?? unchanged).

<!-- Segment lookup (src/lib/data.ts): -->
getSegmentFile(key) is case-insensitive; key = path after "/segments/" without extension, e.g. "azure/caf-workloads", "core/environment", "core/region", "core/instance".
Existing core libraries: src/data/segments/core/{environment,region,instance,...}.json.

<!-- Locale resolution (src/lib/segments.ts): -->
data.rule.<id>.name/.description — convention card
data.field.<FieldName>.label/.tip — field label/tip (JSON tip is the EN fallback)
data.lib.<library>.<value> — option descriptions ("value - description")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: CAF workload library + Key Vault, NSG, Public IP conventions</name>
  <files>src/data/segments/azure/caf-workloads.json, src/rules/azure/azure-key-vault.json, src/rules/azure/azure-network-security-group.json, src/rules/azure/azure-public-ip.json</files>
  <read_first>
    - src/rules/azure/azure-resource-group.json (hyphenated Azure analog — copy its field/builder/validation rhythm)
    - src/rules/purview/purview-sensitivity-label.json (tips + library-backed field mechanism)
    - src/types/Rule.ts (schema limits — confirm every knob used exists)
    - src/data/segments/core/workload.json (shape of a values[] library)
    - src/lib/data-integrity.test.ts (gates your files must pass: library resolution, pattern-token→field mapping, required shape)
  </read_first>
  <action>
Create `src/data/segments/azure/caf-workloads.json` (per D-12 — do NOT edit core/workload.json):

```json
{
  "name": "Workload",
  "values": [
    { "value": "st", "description": "Storage Account" },
    { "value": "kv", "description": "Key Vault" },
    { "value": "nsg", "description": "Network Security Group" },
    { "value": "pip", "description": "Public IP" },
    { "value": "vm", "description": "Virtual Machine" },
    { "value": "vnet", "description": "Virtual Network" },
    { "value": "nic", "description": "Network Interface" },
    { "value": "app", "description": "App Service" }
  ]
}
```

Create the three hyphenated CAF conventions (D-11 style; baseline Azure validation `{"maxLength": 128, "allowedPattern": "^[A-Za-z0-9_.-]+$"}` — these three get NO special validation per CONTEXT):

**src/rules/azure/azure-key-vault.json** (AZURE-02):
```json
{
  "id": "azure-key-vault",
  "category": "Azure",
  "name": "Key Vault",
  "description": "Azure key vault naming per CAF. Vault names are publicly resolvable and globally unique: 3–24 characters, must start with a letter, end with a letter or number, and never contain consecutive hyphens.",
  "pattern": "kv-[Workload]-[Environment]-[Region]",
  "fields": [
    { "name": "Workload", "library": "azure/caf-workloads", "defaultValue": "app", "examples": ["payroll"], "allowCustomValue": true, "tip": "Use your application or workload identifier; CAF abbreviations (st, kv, vm…) are suggested." },
    { "name": "Environment", "library": "core/environment", "defaultValue": "PROD", "allowCustomValue": true },
    { "name": "Region", "library": "core/region", "defaultValue": "FRC", "allowCustomValue": true }
  ],
  "examples": ["KV-APP-PROD-FRC"],
  "builder": {
    "separator": "-",
    "defaultSegments": ["Workload", "Environment", "Region"],
    "availableSegments": ["Custom"],
    "recommendedSegments": ["Workload"],
    "lockedSegments": [],
    "fixedFirstSegments": ["Workload"],
    "fixedLastSegments": []
  },
  "validation": { "maxLength": 24, "allowedPattern": "^[A-Za-z][A-Za-z0-9-]*[A-Za-z0-9]$" }
}
```
The regex hard-enforces start-letter/end-letter-or-number anchors (D-09/D-10 hard part). The 3-char minimum and no-consecutive-hyphens sub-rules are TIPS ONLY — they live in the `description` above (D-10: cannot combine cleanly with the anchors in one readable regex; schema minLength was explicitly rejected). The `kv-` literal prefix rides in the pattern string (literalPrefixes mechanism, useAppState.ts:230-241) — no Prefix field needed.

**src/rules/azure/azure-network-security-group.json** (AZURE-03):
```json
{
  "id": "azure-network-security-group",
  "category": "Azure",
  "name": "Network Security Group",
  "description": "Azure network security group naming per CAF: nsg-<workload>-<instance>.",
  "pattern": "nsg-[Workload]-[Instance]",
  "fields": [
    { "name": "Workload", "library": "azure/caf-workloads", "defaultValue": "app", "allowCustomValue": true },
    { "name": "Instance", "library": "core/instance", "defaultValue": "001", "allowCustomValue": true }
  ],
  "examples": ["NSG-APP-001"],
  "builder": {
    "separator": "-",
    "defaultSegments": ["Workload", "Instance"],
    "availableSegments": ["Custom"],
    "recommendedSegments": ["Workload"],
    "lockedSegments": [],
    "fixedFirstSegments": ["Workload"],
    "fixedLastSegments": []
  },
  "validation": { "maxLength": 128, "allowedPattern": "^[A-Za-z0-9_.-]+$" }
}
```

**src/rules/azure/azure-public-ip.json** (AZURE-04):
```json
{
  "id": "azure-public-ip",
  "category": "Azure",
  "name": "Public IP",
  "description": "Azure public IP naming per CAF: pip-<workload>-<environment>-<region>-<instance>.",
  "pattern": "pip-[Workload]-[Environment]-[Region]-[Instance]",
  "fields": [
    { "name": "Workload", "library": "azure/caf-workloads", "defaultValue": "app", "allowCustomValue": true },
    { "name": "Environment", "library": "core/environment", "defaultValue": "PROD", "allowCustomValue": true },
    { "name": "Region", "library": "core/region", "defaultValue": "FRC", "allowCustomValue": true },
    { "name": "Instance", "library": "core/instance", "defaultValue": "001", "allowCustomValue": true }
  ],
  "examples": ["PIP-APP-PROD-FRC-001"],
  "builder": {
    "separator": "-",
    "defaultSegments": ["Workload", "Environment", "Region", "Instance"],
    "availableSegments": ["Custom"],
    "recommendedSegments": ["Workload"],
    "lockedSegments": [],
    "fixedFirstSegments": ["Workload"],
    "fixedLastSegments": []
  },
  "validation": { "maxLength": 128, "allowedPattern": "^[A-Za-z0-9_.-]+$" }
}
```

Regex safety (T-14-01): every `allowedPattern` above is an anchored simple character-class expression with NO nested quantifiers and NO lookahead — ReDoS-safe by construction. Do not introduce alternation-overlap or nested-quantifier patterns.
  </action>
  <verify>
    <automated>node -e "
const kv=require('./src/rules/azure/azure-key-vault.json');
const nsg=require('./src/rules/azure/azure-network-security-group.json');
const pip=require('./src/rules/azure/azure-public-ip.json');
const lib=require('./src/data/segments/azure/caf-workloads.json');
if(kv.id!=='azure-key-vault'||kv.category!=='Azure') throw 'kv';
if(nsg.pattern!=='nsg-[Workload]-[Instance]') throw 'nsg pattern';
if(pip.pattern!=='pip-[Workload]-[Environment]-[Region]-[Instance]') throw 'pip pattern';
if(!/^\\^[A-Za-z][A-Za-z0-9-]*[A-Za-z0-9]\$/.test('KV-APP-PROD-FRC')) throw 'kv regex rejects valid example';
if(/^\\^[A-Za-z][A-Za-z0-9-]*[A-Za-z0-9]\$/.test('KV--X')) console.log('warn: double-hyphen passes anchors (tip-only per D-10)');
if(!lib.values.some(v=>v.value==='st')) throw 'lib';
console.log('OK: kv/nsg/pip + caf-workloads valid');
"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist and parse as JSON (node require succeeds)
    - caf-workloads.json has exactly 8 values including "st" and "pip"
    - azure-key-vault.json validation block contains exactly `"maxLength": 24` and `"allowedPattern": "^[A-Za-z][A-Za-z0-9-]*[A-Za-z0-9]$"`
    - NSG pattern is `nsg-[Workload]-[Instance]`; Public IP pattern is `pip-[Workload]-[Environment]-[Region]-[Instance]`
    - Every `field.library` value is one of: `azure/caf-workloads`, `core/environment`, `core/region`, `core/instance` (all resolve on disk)
    - Every pattern token ([Workload], [Environment], [Region], [Instance]) has a same-named field in its convention (data-integrity gate)
    - No `allowedPattern` in the 4 files contains nested quantifiers or lookahead: `grep -n 'allowedPattern' src/rules/azure/*.json` shows only anchored class expressions
    - src/data/validation-rules.json is UNCHANGED: `git diff --stat src/data/validation-rules.json` is empty
    - core/workload.json is UNCHANGED
  </acceptance_criteria>
  <done>caf-workloads library + 3 hyphenated CAF conventions on disk, valid JSON, integrity-gate compatible, zero code changes</done>
</task>

<task type="auto">
  <name>task 2: Storage Account outlier convention (no-separator, lowercase-only)</name>
  <files>src/rules/azure/azure-storage-account.json</files>
  <read_first>
    - src/hooks/useAppState.ts lines 225-280 (rawName assembly: pattern literals ride with tokens; separator ?? "-" default; caseMode ?? "upper" pre-normalize; normalizeName applies forceLowercase/removeCharacters/maxLength)
    - src/lib/validation.ts (precedence: validation-rules.json["Azure"]["Storage Account"] WINS over convention.validation)
    - src/data/validation-rules.json (the shadowing block — read its EXACT content)
    - src/data/segments/azure/caf-workloads.json (created in task 1)
    - .planning/phases/14-category-expansions-azure-caf-resources-intune/14-CONTEXT.md (D-01..D-04)
  </read_first>
  <action>
Create `src/rules/azure/azure-storage-account.json` implementing D-01..D-04 exactly:

```json
{
  "id": "azure-storage-account",
  "category": "Azure",
  "name": "Storage Account",
  "description": "Azure storage account naming per CAF. The storage account name IS the public endpoint (e.g. stprovad001.blob.core.windows.net) and must be globally unique across all Azure tenants — uniqueness itself cannot be checked here. Lowercase letters and numbers only, no hyphens, maximum 24 characters.",
  "pattern": "st[Workload][Environment][Instance]",
  "fields": [
    { "name": "Workload", "library": "azure/caf-workloads", "defaultValue": "app", "allowCustomValue": true, "tip": "Use the CAF workload abbreviation (st, kv, app…) or your own short workload code — it concatenates directly into the endpoint hostname." },
    { "name": "Environment", "library": "core/environment", "defaultValue": "prod", "allowCustomValue": true },
    { "name": "Instance", "library": "core/instance", "defaultValue": "001", "allowCustomValue": true }
  ],
  "examples": ["stappprod001"],
  "builder": {
    "separator": "",
    "defaultSegments": ["Workload", "Environment", "Instance"],
    "availableSegments": ["Custom"],
    "recommendedSegments": ["Workload"],
    "lockedSegments": [],
    "fixedFirstSegments": ["Workload"],
    "fixedLastSegments": []
  },
  "validation": {
    "maxLength": 24,
    "forceLowercase": true,
    "removeCharacters": ["-", "_", "."],
    "allowedPattern": "^[a-z0-9]+$"
  }
}
```

Implementation notes (why each oddity is REQUIRED):
- **`"separator": ""`** (not omitted): useAppState.ts:227 reads `builder?.separator ?? "-"` — omitting the key would silently default to hyphen and break D-01 direct concatenation for dynamically added segments. Empty string passes `??` unchanged.
- **Convention name MUST be exactly `"Storage Account"`** in category `"Azure"`: `validation-rules.json` already holds an `"Azure" > "Storage Account"` entry that SHADOWS convention.validation (validation.ts:31). Its content ({maxLength 24, ^[a-z0-9]+$, forceLowercase, removeCharacters [-,_,.]}) is byte-equivalent to the block above — keep the convention block IDENTICAL so precedence is behaviorally irrelevant. DO NOT edit validation-rules.json (13-D-12).
- **Uppercase input flow** (D-03): segment values are upper-cased pre-normalize (caseMode default "upper"), then `forceLowercase` in normalizeName lowers them; `removeCharacters` strips any typed `-`/`_`/`.`; residual violations fail the `^[a-z0-9]+$` whole-name check in validateName.
- **Uniqueness tip** (D-04): carried in `description` (the name IS the endpoint, `stprovad001.blob.core.windows.net`); client-side uniqueness checking was ruled out.
  </action>
  <verify>
    <automated>npx vitest run --reporter=basic 2>/dev/null | tail -3; node -e "
const sa=require('./src/rules/azure/azure-storage-account.json');
if(sa.builder.separator!=='') throw 'separator must be empty string';
if(sa.name!=='Storage Account'||sa.category!=='Azure') throw 'shadowing key mismatch';
if(sa.validation.allowedPattern!=='^[a-z0-9]+$'||sa.validation.forceLowercase!==true||sa.validation.maxLength!==24) throw 'validation mismatch';
// simulate pipeline: upper-case input -> forceLowercase -> strip -> pattern
let n='ST-APP-PROD-001'.toUpperCase();
for(const c of sa.validation.removeCharacters) n=n.split(c).join('');
n=n.toLowerCase().slice(0,sa.validation.maxLength);
if(!new RegExp(sa.validation.allowedPattern).test(n)) throw 'pipeline produced invalid name: '+n;
if(n!=='STAPPPROD001'.toLowerCase()) throw 'unexpected output '+n;
console.log('OK: storage-account outlier valid, pipeline yields', n);
"</automated>
  </verify>
  <acceptance_criteria>
    - File parses as JSON; `"pattern"` is exactly `st[Workload][Environment][Instance]` (no separators between tokens)
    - `"builder"."separator"` is the empty string `""`
    - Validation block is byte-identical to the `validation-rules.json` `Azure > "Storage Account"` entry (maxLength 24, forceLowercase true, removeCharacters ["-","_","."], allowedPattern "^[a-z0-9]+$")
    - Description contains both the endpoint example string `stprovad001.blob.core.windows.net` and a global-uniqueness statement
    - Simulated pipeline: input `ST-APP-PROD-001` normalizes to `stappprod001` and passes `^[a-z0-9]+$`
    - `git diff --stat src/data/validation-rules.json` remains empty
    - `npm test -- --run` passes with the same 137-test suite green (new file picked up by glob without errors)
  </acceptance_criteria>
  <done>Storage Account convention implements the deliberate no-hyphen/lowercase outlier per D-01..D-04, coexisting correctly with the shadowing validation-rules.json entry</done>
</task>

<task type="auto">
  <name>task 3: EN/FR locale keys + phase-plan verification gates</name>
  <files>src/locales/en.json, src/locales/fr.json</files>
  <read_first>
    - src/locales/en.json (grep existing keys BEFORE adding — see criteria below; file is FLAT key-value, e.g. "data.rule.purview-dlp-policy.description")
    - src/locales/fr.json (mirror structure)
    - src/lib/segments.ts lines 27-64 (which keys are resolved and what the JSON fallbacks are)
  </read_first>
  <action>
Add these FLAT keys to BOTH src/locales/en.json and src/locales/fr.json (identical key sets; FR values are faithful French translations — FR terminology is planner discretion per CONTEXT).

Required NEW keys (16 per language):
- `data.rule.azure-storage-account.name` = "Storage Account" / FR "Compte de stockage"
- `data.rule.azure-storage-account.description` = FR: "Nommage des comptes de stockage Azure selon le CAF. Le nom du compte de stockage EST le point de terminaison public (ex. stprovad001.blob.core.windows.net) et doit être globalement unique parmi tous les locataires Azure — l'unicité ne peut pas être vérifiée ici. Lettres minuscules et chiffres uniquement, sans tiret, 24 caractères maximum."
- `data.rule.azure-key-vault.name` = "Key Vault" / FR "Coffre de clés"
- `data.rule.azure-key-vault.description` = FR: "Nommage des coffres de clés Azure selon le CAF. Les noms de coffres sont publiquement résolubles et globalement uniques : 3 à 24 caractères, doivent commencer par une lettre, se terminer par une lettre ou un chiffre, et ne jamais contenir de tirets consécutifs."
- `data.rule.azure-network-security-group.name` = "Network Security Group" / FR "Groupe de sécurité réseau"
- `data.rule.azure-network-security-group.description` = FR: "Nommage des groupes de sécurité réseau Azure selon le CAF : nsg-<charge de travail>-<instance>."
- `data.rule.azure-public-ip.name` = "Public IP" / FR "IP publique"
- `data.rule.azure-public-ip.description` = FR: "Nommage des IP publiques Azure selon le CAF : pip-<charge de travail>-<environnement>-<région>-<instance>."
- `data.lib.azure/caf-workloads.st` … `.kv`, `.nsg`, `.pip`, `.vm`, `.vnet`, `.nic`, `.app` (8 keys) — FR: "Compte de stockage", "Coffre de clés", "Groupe de sécurité réseau", "IP publique", "Machine virtuelle", "Réseau virtuel", "Interface réseau", "App Service"

CONDITIONAL keys — add ONLY if absent (grep first, do NOT duplicate):
- `data.field.Workload.tip` — if present, leave untouched (Phase 13 reuse precedent); if absent, add EN "Use your application or workload identifier; CAF abbreviations are suggested." + FR translation.
- `data.field.Environment.label/.tip`, `data.field.Region.label/.tip`, `data.field.Instance.label/.tip` — same grep-first rule; skip when present.

Then run the FULL plan verification battery (see verify + acceptance_criteria).
  </action>
  <verify>
    <automated>node -e "
const en=require('./src/locales/en.json'), fr=require('./src/locales/fr.json');
const keys=o=>Object.keys(o);
if(keys(en).length!==keys(fr).length) throw 'parity broken: '+keys(en).length+' vs '+keys(fr).length;
const req=['data.rule.azure-storage-account.name','data.rule.azure-key-vault.name','data.rule.azure-network-security-group.name','data.rule.azure-public-ip.name','data.lib.azure/caf-workloads.st','data.lib.azure/caf-workloads.app'];
for(const k of req){ if(!(k in en)) throw 'missing EN '+k; if(!(k in fr)) throw 'missing FR '+k; }
for(const k of req){ const f=k.replace('.name','.description'); if(!(f in en)) throw 'missing EN '+f; }
console.log('OK: parity '+keys(en).length+'='+keys(fr).length+', all new keys mirrored');
" && npx vitest run 2>&1 | tail -4
    </automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const k=Object.keys; k(require('./src/locales/en.json')).length === k(require('./src/locales/fr.json')).length"` → true (parity holds; baseline was 430=430 before this plan)
    - All 8 mandatory keys (`data.rule.azure-{storage-account,key-vault,network-security-group,public-ip}.name` + `.description`) and 8 lib keys (`data.lib.azure/caf-workloads.{st,kv,nsg,pip,vm,vnet,nic,app}`) exist in BOTH en.json and fr.json
    - No duplicated pre-existing keys: `grep -c '"data.field.Workload' src/locales/en.json` returns the same count as before edits started (1 label and/or 1 tip, not doubled)
    - FR descriptions for storage-account and key-vault contain "24 caractères" and the key-vault one mentions "tirets consécutifs"
    - `npx vitest run` → all tests pass (137 existing + data-integrity gates over the 5 new files)
    - `npm run build` exit 0 and `npm run lint` exit 0 (on Linux run `npm ci` first — Windows-only node_modules bindings, per STATE.md constraints)
    - `ls src/rules/azure | wc -l` → 12 (8 existing + 4 new)
    - Zero non-JSON diffs outside locales: `git status --porcelain | grep -v '.json'` shows nothing under src/
  </acceptance_criteria>
  <done>EN/FR parity holds with all new Azure keys mirrored; build/lint/test green; catalog auto-discovers 12 Azure conventions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rule JSON → app runtime | New `allowedPattern` regexes execute in validateName against user-typed names |
| field tips/descriptions → DOM | Bilingual tip strings render via InfoIcon into React text nodes |
| segment values → generated name | Library values and custom input concatenate into displayed/copied names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | DoS | `validation.allowedPattern` / per-field patterns in new azure-*.json | mitigate | Only anchored simple character-class regexes (`^[...]+$` form, no nested quantifiers, no lookahead/alternation overlap) — ReDoS-safe by construction; verified by grep gate in task 1 |
| T-14-01-02 | Tampering/Spoofing | Tips, descriptions, locale strings | accept | Static author-authored JSON rendered as React text nodes (no dangerouslySetInnerHTML anywhere in src/components) — no injection surface; data ships in-repo |
| T-14-01-03 | Injection | Custom segment values copied to clipboard/markdown export | accept | Existing app-wide behavior; values pass through React escaping for display; clipboard write is plain text. No new code path introduced by this plan |
</threat_model>

<verification>
Phase-plan level checks (run after task 3):
1. `npx vitest run` — all green including data-integrity gates over the 5 new files
2. `npm run build && npm run lint` — both exit 0 (npm ci first on Linux)
3. EN/FR key-count parity script from task 3 passes
4. `git diff --stat src/data/validation-rules.json src/data/segments/core/workload.json` — empty (untouched per 13-D-12/D-12)
5. `ls src/rules/azure/*.json | wc -l` = 12
6. Spot-check: `node -e` simulation of storage pipeline ST-APP-PROD-001 → stappprod001 passes `^[a-z0-9]+$`
</verification>

<success_criteria>
- AZURE-01..04 conventions discoverable in the Azure category navigation purely via dropped JSON (no registration)
- Storage account enforces lowercase-alphanumeric/no-hyphen/≤24 via forceLowercase + removeCharacters + allowedPattern, with the endpoint-uniqueness bilingual tip
- Key vault hard-enforces maxLength 24 + start-letter/end-alnum anchors; 3-char minimum and no-consecutive-hyphens carried as tips
- NSG and Public IP follow CAF hyphenated patterns with baseline validation
- EN/FR parity intact; zero TypeScript changes anywhere in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/14-category-expansions-azure-caf-resources-intune/14-01-SUMMARY.md`
</output>