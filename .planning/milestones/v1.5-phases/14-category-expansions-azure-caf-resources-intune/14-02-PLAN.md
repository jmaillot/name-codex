---
phase: 14-category-expansions-azure-caf-resources-intune
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/rules/intune/intune-autopilot-device-name.json
  - src/rules/intune/intune-group-tag.json
  - src/rules/intune/intune-update-ring.json
  - src/rules/intune/intune-assignment-group.json
  - src/data/segments/intune/autopilot-macros.json
  - src/data/segments/intune/group-tags.json
  - src/data/segments/intune/update-rings.json
  - src/data/segments/intune/assignment-group-prefix.json
  - src/locales/en.json
  - src/locales/fr.json
autonomous: true
requirements: [INTUNE-01, INTUNE-02, INTUNE-03, INTUNE-04]
must_haves:
  truths:
    - "User selects the Intune category and sees 4 new conventions: Autopilot Device Name, Group Tag, Update Ring, Assignment Group"
    - "User builds an Autopilot device-name template capped at 15 typed characters with %SERIAL%/%RAND:x% macros inserted via dropdown and an enrollment-expansion warning tip (D-05, D-06, D-07, D-08)"
    - "User builds a Group Tag from tenant-sample values with custom codes allowed (D-13)"
    - "User builds an update-ring name from the fixed Pilot/Dev/Broad/Production taxonomy (D-14)"
    - "User builds an assignment-group name from the INTUNE-{DEV|USR}-<name> taxonomy via a fixed first segment (D-15)"
    - "Every new EN locale key has an FR mirror (parity count equal)"
  artifacts:
    - path: "src/rules/intune/intune-autopilot-device-name.json"
      provides: "INTUNE-01 Autopilot template convention with patterns[] variants"
      contains: "\"maxLength\": 15"
    - path: "src/rules/intune/intune-group-tag.json"
      provides: "INTUNE-02 Group Tag convention"
      contains: "intune/group-tags"
    - path: "src/rules/intune/intune-update-ring.json"
      provides: "INTUNE-03 update ring convention"
      contains: "intune/update-rings"
    - path: "src/rules/intune/intune-assignment-group.json"
      provides: "INTUNE-04 assignment group convention"
      contains: "intune/assignment-group-prefix"
    - path: "src/data/segments/intune/autopilot-macros.json"
      provides: "%SERIAL% + %RAND:1%..%RAND:15% macro library"
      contains: "\"value\": \"%SERIAL%\""
  key_links:
    - from: "src/rules/intune/intune-autopilot-device-name.json"
      to: "src/data/segments/intune/autopilot-macros.json"
      via: "field.library reference"
      pattern: "intune/autopilot-macros"
    - from: "src/rules/intune/*.json"
      to: "src/lib/data-integrity.test.ts"
      via: "pattern tokens map to same-named fields; libraries resolve"
      pattern: "\\[Prefix\\]|\\[GroupTag\\]|\\[UpdateRing\\]|\\[GroupPrefix\\]"
---

<objective>
Add the 4 Intune conventions (Autopilot device-name template with %SERIAL%/%RAND:x% macros, Group Tag, Update Ring, Assignment Group) as pure data-driven JSON to the existing `src/rules/intune/` directory, backed by 4 new segment libraries, fully bilingual EN/FR — zero TypeScript changes (per QUAL-01, D-05..D-15).

Purpose: Deliverable 14b of the roadmap — exercises the hard 15-char macro-expansion-aware budget and patterns[] variants.

Output: 8 new JSON files + extended `en.json`/`fr.json`; auto-discovered by the existing `import.meta.glob`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-category-expansions-azure-caf-resources-intune/14-CONTEXT.md
@.planning/phases/14-category-expansions-azure-caf-resources-intune/14-PATTERNS.md
@.planning/phases/13-new-microsoft-365-categories-teams-sharepoint-power-platform/13-01-SUMMARY.md
@src/rules/purview/purview-sensitivity-label.json
@src/rules/intune/endpoint-security-policy.json

<interfaces>
<!-- Schema knobs (src/types/Rule.ts) — NO code changes: -->
NamingValidationConfig: { maxLength?, allowedPattern?, forceLowercase?, removeCharacters?: string[], caseMode?: "upper"|"lower"|"preserve" }
NamingBuilderConfig:   { separator?, defaultSegments[], availableSegments[], recommendedSegments[], lockedSegments[], fixedFirstSegments[], fixedLastSegments[] }
NamingField:           { name, label?, type?, library?, defaultValue?, allowedValues?, allowCustomValue?, allowedPattern?, tip?, examples?, generator? }
NamingPatternOption:   { id, name, description, pattern, defaultSegments[], examples[] } on convention.patterns[]

<!-- Precedent for one-convention-many-patterns: purview-sensitivity-label.json (patterns[] variants; localized variant names via data.rule.<id>.pattern.<variantId>.name, patternName() at segments.ts:62). -->

<!-- Separator resolution (useAppState.ts:227): omitted separator defaults to "-"; use "" for direct concatenation. -->

<!-- Library lookup (data.ts): key = path after "/segments/" without ext, e.g. "intune/autopilot-macros". data-integrity.test.ts fails if a referenced library file is missing or empty. -->

<!-- Existing intune libraries NOT to be touched: intune/intune-prefix.json (IntPrefix: CMP/CFG/MAM/MACFG) keeps its existing consumers untouched per D-15 discretion — this plan adds a SIBLING library instead of extending it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Four Intune segment libraries</name>
  <files>src/data/segments/intune/autopilot-macros.json, src/data/segments/intune/group-tags.json, src/data/segments/intune/update-rings.json, src/data/segments/intune/assignment-group-prefix.json</files>
  <read_first>
    - src/data/segments/intune/intune-scopetag.json (minimal library shape analog)
    - src/data/segments/core/workload.json (descriptive-values shape; descriptions surface in optionLabel as "value - description")
    - src/lib/data-integrity.test.ts lines 10-21 (library resolution gate)
    - .planning/phases/14-category-expansions-azure-caf-resources-intune/14-CONTEXT.md (D-06, D-13, D-14, D-15)
  </read_first>
  <action>
Create exactly these four files:

**src/data/segments/intune/autopilot-macros.json** (D-06 — prevents macro typos; 16 values):
```json
{
  "name": "Macro",
  "values": [
    { "value": "%SERIAL%", "description": "Device serial number, expanded at enrollment" },
    { "value": "%RAND:1%", "description": "1 random character" },
    { "value": "%RAND:2%", "description": "2 random characters" },
    { "value": "%RAND:3%", "description": "3 random characters" },
    { "value": "%RAND:4%", "description": "4 random characters" },
    { "value": "%RAND:5%", "description": "5 random characters" },
    { "value": "%RAND:6%", "description": "6 random characters" },
    { "value": "%RAND:7%", "description": "7 random characters" },
    { "value": "%RAND:8%", "description": "8 random characters" },
    { "value": "%RAND:9%", "description": "9 random characters" },
    { "value": "%RAND:10%", "description": "10 random characters" },
    { "value": "%RAND:11%", "description": "11 random characters" },
    { "value": "%RAND:12%", "description": "12 random characters" },
    { "value": "%RAND:13%", "description": "13 random characters" },
    { "value": "%RAND:14%", "description": "14 random characters" },
    { "value": "%RAND:15%", "description": "15 random characters" }
  ]
}
```

**src/data/segments/intune/group-tags.json** (D-13 — tenant-specific samples):
```json
{
  "name": "GroupTag",
  "values": [
    { "value": "DEV", "description": "Development department" },
    { "value": "HR", "description": "Human Resources" },
    { "value": "FIN", "description": "Finance" },
    { "value": "OPS", "description": "Operations" },
    { "value": "PARIS", "description": "Paris site" },
    { "value": "LONDON", "description": "London site" }
  ]
}
```

**src/data/segments/intune/update-rings.json** (D-14 — fixed ring taxonomy):
```json
{
  "name": "UpdateRing",
  "values": [
    { "value": "Pilot", "description": "Pilot ring — early adopters" },
    { "value": "Dev", "description": "Development devices" },
    { "value": "Broad", "description": "Broad deployment" },
    { "value": "Production", "description": "Production devices" }
  ]
}
```

**src/data/segments/intune/assignment-group-prefix.json** (D-15 sibling-library choice — do NOT extend intune-prefix.json, whose CMP/CFG/MAM/MACFG consumers must not see the new tokens):
```json
{
  "name": "GroupPrefix",
  "values": [
    { "value": "INTUNE-DEV", "description": "Device assignment group" },
    { "value": "INTUNE-USR", "description": "User assignment group" }
  ]
}
```
  </action>
  <verify>
    <automated>node -e "
const m=require('./src/data/segments/intune/autopilot-macros.json');
if(m.values.length!==16) throw 'expected 16 macros';
if(!m.values.some(v=>v.value==='%SERIAL%')) throw 'no SERIAL';
if(!m.values.some(v=>v.value==='%RAND:15%')) throw 'no RAND:15';
for(const f of ['group-tags','update-rings','assignment-group-prefix']){
  const j=require('./src/data/segments/intune/'+f+'.json');
  if(!j.values.length) throw f+' empty';
}
console.log('OK: 4 intune libraries valid');
"</automated>
  </verify>
  <acceptance_criteria>
    - autopilot-macros.json has exactly 16 values: %SERIAL% plus %RAND:1% through %RAND:15%, each with a non-empty description
    - update-rings.json has exactly 4 values: Pilot, Dev, Broad, Production
    - assignment-group-prefix.json has exactly 2 values: INTUNE-DEV, INTUNE-USR
    - All 4 files parse as JSON and follow `{name, values[{value, description}]}` shape
    - src/data/segments/intune/intune-prefix.json is UNCHANGED (`git diff --stat` on it is empty)
  </acceptance_criteria>
  <done>Four segment libraries on disk resolving at keys intune/autopilot-macros, intune/group-tags, intune/update-rings, intune/assignment-group-prefix</done>
</task>

<task type="auto">
  <name>task 2: Autopilot device-name convention (macro-aware outlier)</name>
  <files>src/rules/intune/intune-autopilot-device-name.json</files>
  <read_first>
    - src/rules/purview/purview-sensitivity-label.json (patterns[] variants mechanism — exact precedent)
    - src/hooks/useAppState.ts lines 225-280 (literal handling inside pattern strings; maxLength slices typed text)
    - src/lib/validation.ts (whole-name allowedPattern check runs against normalized generated name)
    - .planning/phases/14-category-expansions-azure-caf-resources-intune/14-CONTEXT.md (D-05..D-08)
  </read_first>
  <action>
Create `src/rules/intune/intune-autopilot-device-name.json` implementing D-05..D-08 exactly:

```json
{
  "id": "intune-autopilot-device-name",
  "category": "Intune",
  "name": "Autopilot Device Name",
  "description": "Autopilot device-name template. Hard limit: 15 characters total — Windows enforces it AFTER %SERIAL% / %RAND:n% macros expand at enrollment, so keep static text well under budget. Allowed characters: letters, numbers, hyphens; the name may not consist entirely of numbers.",
  "pattern": "[Prefix][Macro]",
  "patterns": [
    {
      "id": "prefix-serial",
      "name": "Prefix + Serial",
      "description": "Static prefix plus the %SERIAL% macro (e.g. DT-%SERIAL%)",
      "pattern": "[Prefix][Macro]",
      "defaultSegments": ["Prefix", "Macro"],
      "examples": ["DT-%SERIAL%"]
    },
    {
      "id": "prefix-random",
      "name": "Prefix + Random",
      "description": "Static prefix plus a %RAND:n% macro",
      "pattern": "[Prefix][Macro]",
      "defaultSegments": ["Prefix", "Macro"],
      "examples": ["LT-%RAND:6%"]
    },
    {
      "id": "custom-template",
      "name": "Custom Template",
      "description": "Free-form template mixing static text and macros",
      "pattern": "[Template]",
      "defaultSegments": ["Template"],
      "examples": ["DT-%RAND:4%%SERIAL%"]
    }
  ],
  "fields": [
    {
      "name": "Prefix",
      "type": "text",
      "defaultValue": "DT-",
      "examples": ["DT-", "LT-", "WKS-"],
      "allowCustomValue": true,
      "allowedPattern": "^[A-Za-z0-9-]+$",
      "tip": "Letters, numbers and hyphens only. Static text counts toward the 15-character limit — %SERIAL% expands to the real serial at enrollment, so leave room."
    },
    {
      "name": "Macro",
      "library": "intune/autopilot-macros",
      "defaultValue": "%SERIAL%",
      "allowCustomValue": true,
      "tip": "Pick the macro from the list to avoid typos; custom macro strings are allowed. Macros expand AFTER the app checks your name against the 15-character limit."
    },
    {
      "name": "Template",
      "type": "text",
      "defaultValue": "",
      "allowCustomValue": true,
      "allowedPattern": "^[A-Za-z0-9%:-]+$",
      "tip": "Mix static text and macros (%SERIAL%, %RAND:n%). Count every character as typed — %SERIAL% is 8 characters today but grows at enrollment. The name may not be all numbers."
    }
  ],
  "examples": ["DT-%SERIAL%"],
  "builder": {
    "separator": "",
    "defaultSegments": ["Prefix", "Macro"],
    "availableSegments": ["Macro", "Custom"],
    "recommendedSegments": [],
    "lockedSegments": [],
    "fixedFirstSegments": ["Prefix"],
    "fixedLastSegments": []
  },
  "validation": {
    "maxLength": 15,
    "allowedPattern": "^[A-Za-z0-9%:-]+$"
  }
}
```

Implementation notes (each oddity is REQUIRED by a decision):
- **maxLength 15 counts LITERAL typed text** (D-05): `DT-%SERIAL%` = 11 chars as typed. Macro-expansion-aware estimation was explicitly REJECTED — no code.
- **Colon in allowedPattern** (D-07 charset split): `%RAND:6%` contains `:`, so the whole-name pattern admits `[A-Za-z0-9%:-]`. The not-all-numbers rule stays a TIP (lookahead regex rejected as opaque checklist noise) — carried in description + Template.tip.
- **Macros via dropdown library** (D-06): Macro field references task 1's `intune/autopilot-macros`; `allowCustomValue: true` keeps custom templates possible.
- **`"separator": ""`**: direct concatenation so Prefix value `DT-` + Macro `%SERIAL%` yields `DT-%SERIAL%` (omitted separator would default to `-` at useAppState.ts:227).
- **patterns[] variants** (D-08): one convention, three variants per the Phase 13 D-14 precedent; localized variant names come from `data.rule.intune-autopilot-device-name.pattern.<variantId>.name` (added in task 3).
- Regex safety (T-14-02-01): both patterns are anchored simple classes — no lookahead, no nested quantifiers.
  </action>
  <verify>
    <automated>npx vitest run src/lib/data-integrity.test.ts 2>&1 | tail -3; node -e "
const a=require('./src/rules/intune/intune-autopilot-device-name.json');
if(a.validation.maxLength!==15) throw 'maxLength';
if(!a.patterns||a.patterns.length!==3) throw 'need 3 variants';
const ids=a.patterns.map(p=>p.id);
for(const i of ['prefix-serial','prefix-random','custom-template']) if(!ids.includes(i)) throw 'missing '+i;
if(a.builder.separator!=='') throw 'separator';
// typed-length simulation: DT-%SERIAL% = 11 chars <= 15
if('DT-%SERIAL%'.length!==11 || !new RegExp(a.validation.allowedPattern).test('DT-%SERIAL%')) throw 'example fails own validation';
if(new RegExp(a.validation.allowedPattern).test('%RAND:6%')!==true) throw 'RAND rejected';
console.log('OK: autopilot convention valid');
"</automated>
  </verify>
  <acceptance_criteria>
    - File parses; validation block is exactly `{"maxLength": 15, "allowedPattern": "^[A-Za-z0-9%:-]+$"}`
    - Exactly 3 patterns[] variants with ids prefix-serial, prefix-random, custom-template; each maps its pattern tokens ([Prefix], [Macro], [Template]) to same-named fields (data-integrity gate passes)
    - `"builder"."separator"` is `""`
    - Macro field has `"library": "intune/autopilot-macros"` and `"allowCustomValue": true`
    - Prefix field allowedPattern is `^[A-Za-z0-9-]+$` (no colon — colon only legal via macros)
    - Description contains "15 characters" and the not-all-numbers statement; Prefix.tip contains "expands"; Template.tip contains "all numbers"
    - Example `DT-%SERIAL%` passes the whole-name allowedPattern regex and is ≤ 15 chars as typed
  </acceptance_criteria>
  <done>Autopilot convention implements D-05..D-08: literal-counted 15-char budget, macro dropdown, charset split, 3 variants</done>
</task>

<task type="auto">
  <name>task 3: Group Tag + Update Ring + Assignment Group conventions</name>
  <files>src/rules/intune/intune-group-tag.json, src/rules/intune/intune-update-ring.json, src/rules/intune/intune-assignment-group.json</files>
  <read_first>
    - src/rules/intune/endpoint-security-policy.json (library-backed fields, fixedFirstSegments model)
    - src/data/segments/intune/assignment-group-prefix.json, update-rings.json, group-tags.json (created in task 1)
    - .planning/phases/14-category-expansions-azure-caf-resources-intune/14-CONTEXT.md (D-13..D-15)
  </read_first>
  <action>
Create three conventions following the endpoint-security-policy.json rhythm (baseline Intune validation, no special cases):

**src/rules/intune/intune-group-tag.json** (INTUNE-02):
```json
{
  "id": "intune-group-tag",
  "category": "Intune",
  "name": "Group Tag",
  "description": "Autopilot Group Tag: a department or site code that drives dynamic group membership and per-group naming profiles. Values are tenant-specific — samples provided, custom codes expected.",
  "pattern": "[GroupTag]",
  "fields": [
    {
      "name": "GroupTag",
      "library": "intune/group-tags",
      "defaultValue": "DEV",
      "allowCustomValue": true,
      "tip": "Group Tags are tenant-specific: use your own department/site codes. One tag per device feeds dynamic group rules."
    }
  ],
  "examples": ["DEV"],
  "builder": {
    "separator": "-",
    "defaultSegments": ["GroupTag"],
    "availableSegments": ["Custom"],
    "recommendedSegments": [],
    "lockedSegments": [],
    "fixedFirstSegments": [],
    "fixedLastSegments": []
  },
  "validation": { "maxLength": 128, "allowedPattern": "^[A-Za-z0-9_.-]+$" }
}
```

**src/rules/intune/intune-update-ring.json** (INTUNE-03 — ring taxonomy locked to the library, `allowCustomValue: false` per D-14):
```json
{
  "id": "intune-update-ring",
  "category": "Intune",
  "name": "Update Ring",
  "description": "Windows Update ring naming using the fixed deployment-ring taxonomy: Pilot, Dev, Broad, Production.",
  "pattern": "[UpdateRing]-[Descriptor]",
  "fields": [
    {
      "name": "UpdateRing",
      "library": "intune/update-rings",
      "defaultValue": "Pilot",
      "allowCustomValue": false
    },
    {
      "name": "Descriptor",
      "type": "text",
      "defaultValue": "",
      "examples": ["Windows11", "FeatureUpdates"],
      "allowCustomValue": true,
      "allowedPattern": "^[A-Za-z0-9-]+$"
    }
  ],
  "examples": ["Pilot-Windows11"],
  "builder": {
    "separator": "-",
    "defaultSegments": ["UpdateRing", "Descriptor"],
    "availableSegments": ["Custom"],
    "recommendedSegments": ["Descriptor"],
    "lockedSegments": [],
    "fixedFirstSegments": ["UpdateRing"],
    "fixedLastSegments": []
  },
  "validation": { "maxLength": 128, "allowedPattern": "^[A-Za-z0-9_.-]+$" }
}
```

**src/rules/intune/intune-assignment-group.json** (INTUNE-04 — `INTUNE-{DEV|USR}-<name>` taxonomy pinned via fixed first segment):
```json
{
  "id": "intune-assignment-group",
  "category": "Intune",
  "name": "Assignment Group",
  "description": "Entra assignment-group name for Intune deployments using the INTUNE-{DEV|USR}-<name> taxonomy.",
  "pattern": "[GroupPrefix]-[GroupName]",
  "fields": [
    {
      "name": "GroupPrefix",
      "library": "intune/assignment-group-prefix",
      "defaultValue": "INTUNE-DEV",
      "allowCustomValue": false
    },
    {
      "name": "GroupName",
      "type": "text",
      "defaultValue": "",
      "examples": ["Laptops", "Phones"],
      "allowCustomValue": true,
      "allowedPattern": "^[A-Za-z0-9-]+$"
    }
  ],
  "examples": ["INTUNE-DEV-Laptops"],
  "builder": {
    "separator": "-",
    "defaultSegments": ["GroupPrefix", "GroupName"],
    "availableSegments": ["Custom"],
    "recommendedSegments": ["GroupName"],
    "lockedSegments": [],
    "fixedFirstSegments": ["GroupPrefix"],
    "fixedLastSegments": []
  },
  "validation": { "maxLength": 128, "allowedPattern": "^[A-Za-z0-9_.-]+$" }
}
```

Sibling-library rationale (D-15 planner discretion): extending `intune/intune-prefix.json` (IntPrefix) would surface INTUNE-DEV/INTUNE-USR to CMP/CFG/MAM/MACFG consumers; the dedicated `assignment-group-prefix` library avoids that cross-contamination with zero code changes.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -3; node -e "
const gt=require('./src/rules/intune/intune-group-tag.json');
const ur=require('./src/rules/intune/intune-update-ring.json');
const ag=require('./src/rules/intune/intune-assignment-group.json');
if(gt.fields[0].library!=='intune/group-tags'||gt.fields[0].allowCustomValue!==true) throw 'gt';
if(ur.fields[0].library!=='intune/update-rings'||ur.fields[0].allowCustomValue!==false) throw 'ur';
if(ag.pattern!=='[GroupPrefix]-[GroupName]') throw 'ag pattern';
if(ag.builder.fixedFirstSegments[0]!=='GroupPrefix') throw 'ag fixed first';
console.log('OK: group-tag/update-ring/assignment-group valid');
"</automated>
  </verify>
  <acceptance_criteria>
    - intune-group-tag.json: GroupTag field uses library intune/group-tags with allowCustomValue true; example "DEV" present
    - intune-update-ring.json: UpdateRing field uses library intune/update-rings with allowCustomValue FALSE; pattern `[UpdateRing]-[Descriptor]`; fixedFirstSegments contains "UpdateRing"
    - intune-assignment-group.json: pattern `[GroupPrefix]-[GroupName]`; GroupPrefix library intune/assignment-group-prefix with allowCustomValue false; fixedFirstSegments contains "GroupPrefix"; example "INTUNE-DEV-Laptops"
    - All three keep baseline validation `{"maxLength": 128, "allowedPattern": "^[A-Za-z0-9_.-]+$"}`
    - Every pattern token maps to a same-named field (data-integrity gate green)
    - `npx vitest run` all tests pass; `npm run build && npm run lint` exit 0 (npm ci first on Linux)
    - `ls src/rules/intune | wc -l` = 9 (5 existing + 4 new)
  </acceptance_criteria>
  <done>All four Intune conventions on disk, integrity-gate compatible, build/lint/test green</done>
</task>

<task type="auto">
  <name>task 4: EN/FR locale keys + phase verification gates</name>
  <files>src/locales/en.json, src/locales/fr.json</files>
  <read_first>
    - src/locales/en.json (grep before adding — flat keys; baseline after plan 14-01 will be 430+16=446 keys per language)
    - src/locales/fr.json (mirror)
    - src/lib/segments.ts lines 27-64 (key resolution incl. patternName → data.rule.<id>.pattern.<variantId>.name)
  </read_first>
  <action>
Add these FLAT keys to BOTH en.json and fr.json (identical key sets; FR translations required):

Rule cards (8 keys): `data.rule.{intune-autopilot-device-name,intune-group-tag,intune-update-ring,intune-assignment-group}.name/.description` — copy the English values from tasks 2-3 JSON; FR examples: "Nom d'appareil Autopilot", "Balise de groupe", "Anneau de mise à jour", "Groupe d'affectation"; translate the descriptions faithfully (keep "15 caractères", "%SERIAL%", "%RAND:n%", "Pilot, Dev, Broad, Production", "INTUNE-{DEV|USR}-<nom>" literals intact).

Variant names (3 keys): `data.rule.intune-autopilot-device-name.pattern.prefix-serial.name` = "Prefix + Serial"/"Préfixe + série", `.pattern.prefix-random.name` = "Prefix + Random"/"Préfixe + aléatoire", `.pattern.custom-template.name` = "Custom Template"/"Modèle personnalisé".

Field tips (4 keys, EN fallbacks already in JSON — these become the authoritative bilingual entries):
- `data.field.Prefix.tip`, `data.field.Macro.tip`, `data.field.Template.tip` — FR translations of the task-2 tips ("se développe à l'inscription", "15 caractères", etc.)
- `data.field.GroupTag.tip` — FR translation of the task-3 tip

Lib descriptions (30 keys): use the literal value as key suffix exactly as stored: `data.lib.intune/autopilot-macros.%SERIAL%`, `...%RAND:1%` through `...%RAND:15%` (16 keys); `data.lib.intune/group-tags.{DEV,HR,FIN,OPS,PARIS,LONDON}` (6); `data.lib.intune/update-rings.{Pilot,Dev,Broad,Production}` (4); `data.lib.intune/assignment-group-prefix.{INTUNE-DEV,INTUNE-USR}` (2). Keys containing `%`/`:` are unusual — spot-check at runtime that a macro option label renders its description; if the i18n lookup mishandles those characters, skip all 16 macro lib keys on BOTH sides (parity is what matters) and document the choice in the SUMMARY.

Total ≈ 45 keys per language. Grep-first rule: skip any key that already exists (do not duplicate).
  </action>
  <verify>
    <automated>node -e "
const en=require('./src/locales/en.json'), fr=require('./src/locales/fr.json');
const k=o=>Object.keys(o).length;
if(k(en)!==k(fr)) throw 'parity broken: '+k(en)+' vs '+k(fr);
const req=['data.rule.intune-autopilot-device-name.name','data.rule.intune-autopilot-device-name.description','data.rule.intune-group-tag.name','data.rule.intune-update-ring.name','data.rule.intune-assignment-group.name','data.rule.intune-autopilot-device-name.pattern.prefix-serial.name','data.rule.intune-autopilot-device-name.pattern.custom-template.name'];
for(const x of req){ if(!(x in en)) throw 'missing EN '+x; if(!(x in fr)) throw 'missing FR '+x; }
for(const v of ['DEV','HR','FIN']){ const x='data.lib.intune/group-tags.'+v; if(!(x in en)||!(x in fr)) throw 'missing '+x; }
for(const v of ['Pilot','Production']){ const x='data.lib.intune/update-rings.'+v; if(!(x in en)||!(x in fr)) throw 'missing '+x; }
console.log('OK: parity '+k(en)+'='+k(fr)+', intune keys mirrored');
"</automated>
  </verify>
  <acceptance_criteria>
    - EN and FR key counts equal (node script above exits clean)
    - All 4 rule name/description pairs exist in both languages
    - All 3 autopilot variant-name keys exist in both languages
    - group-tags (6), update-rings (4), assignment-group-prefix (2) lib keys mirrored; autopilot-macros lib keys either fully mirrored on BOTH sides or skipped on BOTH sides (parity holds either way; choice documented in SUMMARY)
    - FR rule names contain no leftover English for the four conventions (e.g. "Balise de groupe", not "Group Tag")
    - Full battery: `npx vitest run` green · `npm run build` exit 0 · `npm run lint` exit 0
    - `git status --porcelain | grep -v '.json'` shows nothing under src/ (zero code changes, QUAL-01 precondition)
    - `ls src/rules/intune/*.json | wc -l` = 9; `ls src/data/segments/intune/*.json | wc -l` = 8 (4 existing + 4 new)
  </acceptance_criteria>
  <done>EN/FR parity holds with all new Intune keys mirrored; full gates green; catalog auto-discovers 9 Intune conventions with zero code changes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rule JSON → app runtime | New `allowedPattern` regexes execute in validateName against user-typed names |
| field tips/descriptions → DOM | Bilingual tip strings render via InfoIcon into React text nodes |
| macro/custom values → generated name & clipboard | Custom template text (incl. `%`/`:` chars) concatenates into displayed/copied names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | DoS | allowedPattern regexes in intune-*.json (incl. `%`/`:` admitting pattern) | mitigate | Only anchored simple character-class regexes (`^[...]+$`), no nested quantifiers, no lookahead (lookahead explicitly rejected per D-07) — ReDoS-safe by construction |
| T-14-02-02 | Tampering/Spoofing | Tips, descriptions, variant names, locale strings | accept | Static authored JSON rendered as React text nodes; no dangerouslySetInnerHTML in any component — no injection path |
| T-14-02-03 | Injection | Custom Template free text flowing to clipboard/markdown export | accept | Plain-text clipboard write (existing module-scope helper, Phase 04 hardened); React escaping covers display; no new code path |
| T-14-02-04 | Repudiation | Macro expansion mismatch (typed length ≠ enrolled length) | accept | Documented product behavior per D-05: literal-count enforcement + bilingual expansion warning tips; over-long names fail at enrollment, not silently corrupted here |
</threat_model>

<verification>
Phase-plan level checks (run after task 4):
1. `npx vitest run` — all green including data-integrity gates over the 8 new files
2. `npm run build && npm run lint` — both exit 0 (npm ci first on Linux)
3. EN/FR parity script from task 4 passes
4. `ls src/rules/intune/*.json | wc -l` = 9; `ls src/data/segments/intune/*.json | wc -l` = 8
5. `git status --porcelain` — no modified files outside *.json under src/
6. Spot-check: `DT-%SERIAL%` (11 typed chars) passes `^[A-Za-z0-9%:-]+$` and maxLength 15
</verification>

<success_criteria>
- INTUNE-01..04 conventions discoverable in the Intune category navigation purely via dropped JSON
- Autopilot template enforces the 15-char literal budget with macro dropdown insertion and expansion/not-all-numbers bilingual tips across 3 patterns[] variants
- Group Tag allows custom tenant codes; Update Ring locks Pilot/Dev/Broad/Production; Assignment Group pins INTUNE-{DEV|USR} via a fixed-first segment backed by a NEW sibling library (IntPrefix untouched)
- EN/FR parity intact; zero TypeScript changes anywhere in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/14-category-expansions-azure-caf-resources-intune/14-02-SUMMARY.md`
</output>
