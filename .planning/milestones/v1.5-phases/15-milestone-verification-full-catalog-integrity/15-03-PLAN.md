---
phase: 15-milestone-verification-full-catalog-integrity
plan: 03
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - .planning/phases/15-milestone-verification-full-catalog-integrity/15-SMOKE-SCRIPT.md
  - .planning/phases/15-milestone-verification-full-catalog-integrity/15-VERIFICATION.md
autonomous: true
requirements: [QUAL-01]
must_haves:
  truths:
    - "D-13: every mechanical gate (npm ci → check:tokens → vitest → build → lint) exits 0 on Linux with verbatim output recorded"
    - "D-05: the zero-code-diff proof runs `git diff v1.4..HEAD --name-only` filtered against the D-06 whitelist and its survivor set equals exactly the pre-declared justified sweep diffs (WR-04..WR-06 files) — nothing else"
    - "D-06: allowed-change whitelist enforced — `src/rules/**`, `src/data/**`, `src/locales/en.json`, `src/locales/fr.json`; any other surviving path must be explicitly justified in 15-VERIFICATION.md"
    - "D-01: the smoke script covers the full dual matrix — 6 workflows × EN/FR × dark/light ≈ 24 runs, reproducible via fixed conventions × fixed segment values"
    - "The consolidated verification report exists with gates, diff-proof, and assertion-evidence sections complete"
  artifacts:
    - path: ".planning/phases/15-milestone-verification-full-catalog-integrity/15-SMOKE-SCRIPT.md"
      provides: "step-by-step user-run smoke script (D-02/D-03/D-04)"
      contains: "teams-project-team"
    - path: ".planning/phases/15-milestone-verification-full-catalog-integrity/15-VERIFICATION.md"
      provides: "consolidated evidence record, mechanical sections complete (D-10)"
      contains: "Zero-code-diff proof"
  key_links:
    - from: "15-SMOKE-SCRIPT.md"
      to: "src/rules/*/*.json"
      via: "each workflow pins one convention id + literal segment values from its libraries"
      pattern: "azure-storage-account|pp-environment|purview-sensitivity-label"
    - from: "15-VERIFICATION.md"
      to: "git diff v1.4..HEAD --name-only"
      via: "diff-filter output quoted verbatim with per-survivor justification"
      pattern: "git diff v1\\.4\\.\\.HEAD"
---

<objective>
Run the full mechanical gate sequence on Linux, execute the D-05/D-06 zero-code-diff proof against tag `v1.4`, author the user-run dual smoke matrix script, and draft the consolidated `15-VERIFICATION.md` with all mechanical sections complete.

Purpose: produce the objective half of QUAL-01's evidence so Plan 15-04 only needs the user-run subjective matrix and the verdict.
Output: `15-SMOKE-SCRIPT.md`, drafted `15-VERIFICATION.md` (gates + diff-proof + assertions done; smoke matrix + verdict pending).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-milestone-verification-full-catalog-integrity/15-CONTEXT.md
@.planning/milestones/v1.4-phases/12-theme-switching-verification/12-SMOKE-MATRIX.md

<interfaces>
<!-- Report format analogs — copy structure, not content -->

12-SMOKE-MATRIX.md structure to emulate: header block (bolded Plan/Requirement/Executed/Method lines) → one subsection per gate with exit code stated + verbatim fenced output + ✅ PASS line → environment preamble noting Linux npm ci → evidence-by-grep sections (criterion + verbatim excerpt) → smoke matrix table with honest per-cell PASS/FAIL + cell-count footer → verdict summary table + closing statement.

Gate sequence (package.json):
```json
"build": "tsc -b && vite build",
"lint": "oxlint",
"check:tokens": "node scripts/check-tokens.mjs",
"pretest": "npm run check:tokens",
"test": "vitest run"
```
Sequence: `npm ci` FIRST (Windows-native node_modules constraint, STATE.md) → `npx vitest run` (pretest auto-runs check:tokens; also run it standalone for explicit evidence) → `npm run build` → `npm run lint`.

Diff-proof command (D-05/D-06):
```bash
git diff v1.4..HEAD --name-only | grep -Ev '^(src/rules/|src/data/|src/locales/en\.json$|src/locales/fr\.json$)'
```
Expected survivor set (pre-declared JUSTIFIED diffs from Plans 15-01/15-02): src/lib/validation.ts, src/hooks/useAppState.ts, src/components/SegmentEditor.tsx. Anything else is a finding.

Known flake to annotate honestly (14-REVIEW-FIX.md §Verification): WSL vitest worker-startup timeouts occasionally hit files before they run — rerun the affected file in isolation rather than hiding it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Run mechanical gate sequence and capture verbatim evidence</name>
  <files>.planning/phases/15-milestone-verification-full-catalog-integrity/15-VERIFICATION.md</files>
  <read_first>
    - package.json (scripts block)
    - .planning/milestones/v1.4-phases/12-theme-switching-verification/12-SMOKE-MATRIX.md lines 1–82 (gate subsection format)
    - .planning/phases/14-category-expansions-azure-caf-resources-intune/14-REVIEW-FIX.md §Verification (WSL flake precedent)
  </read_first>
  <action>
Execute IN THIS ORDER from repo root, capturing exit code + full output of each:

1. `npm ci` — expect exit 0 (Linux node_modules rebuild)
2. `npm run check:tokens` — expect exit 0 (also auto-runs via pretest)
3. `npx vitest run` — expect exit 0; record total test count (143 baseline + new catalog assertions from 15-01). If a worker-startup timeout hits a file, rerun that file in isolation (`npx vitest run <file>`) and annotate BOTH facts in the report.
4. `npm run build` — expect exit 0
5. `npm run lint` — expect exit 0 (warnings allowed by design, errors fail)

Create `.planning/phases/15-milestone-verification-full-catalog-integrity/15-VERIFICATION.md` modeled section-for-section on 12-SMOKE-MATRIX.md: header (`# Phase 15 / v1.5 — Full Milestone Verification Record`), bolded Phase/Requirement (QUAL-01)/Executed date/Method lines, environment preamble ("Linux worktree checkout — `npm ci` run first per STATE.md environment note"), then one subsection per gate in the order above with **exit code stated**, verbatim output in fenced blocks, and a ✅ PASS verdict line. Paste command output inside fenced code blocks ONLY — never raw HTML or interpolated text (script-injection hygiene for the report artifact).
If any gate FAILS mechanically: this is an in-phase-fixable failure (D-08) — diagnose; if caused by 15-01/15-02 changes, fix within those plans' declared files and re-run; otherwise record the FAIL verbatim and stop before task 3.
  </action>
  <verify>
    <automated>grep -c '✅ PASS' .planning/phases/15-milestone-verification-full-catalog-integrity/15-VERIFICATION.md | grep -qx '5\|[6-9]' && grep -q 'exit code' .planning/phases/15-milestone-verification-full-catalog-integrity/15-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 15-VERIFICATION.md exists and contains five gate subsections (npm ci, check:tokens, vitest, build, lint) each stating its exit code and quoting verbatim command output in fenced blocks
    - Each gate subsection ends with a ✅ PASS line (or a documented FAIL + disposition if genuinely unfixable)
    - The environment preamble mentions `npm ci` on Linux / Windows-native node_modules
    - Recorded vitest count ≥ 147 (143 baseline + 4 new catalog tests)
  </acceptance_criteria>
  <done>All five mechanical gates exit 0 with verbatim evidence committed to the report.</done>
</task>

<task type="auto">
  <name>task 2: Zero-code-diff proof (D-05/D-06) + assertion-evidence sections</name>
  <files>.planning/phases/15-milestone-verification-full-catalog-integrity/15-VERIFICATION.md</files>
  <read_first>
    - 15-VERIFICATION.md as written by task 1 (append sections, do not rewrite)
    - git tag list (confirm `v1.4` exists: `git tag | grep v1.4`)
    - .planning/phases/15-milestone-verification-full-catalog-integrity/15-CONTEXT.md D-05/D-06 (whitelist definition)
  </read_first>
  <action>
**Diff proof:** Run exactly (fixed literal patterns — no variable interpolation, no eval; filenames come from our own repo but the filter stays static):
```bash
git diff v1.4..HEAD --name-only | grep -Ev '^(src/rules/|src/data/|src/locales/en\.json$|src/locales/fr\.json$)'
```
Also capture the FULL changed-file list (`git diff v1.4..HEAD --name-only | wc -l` and the filtered whitelist-side summary: counts per directory).

Paste the survivor-list command + output VERBATIM into a new "## Zero-code-diff proof" section. Then add one bullet per survivor justifying it under D-06's "zero **or justified**" clause:
- `src/lib/validation.ts` — WR-04 fix, sanctioned sweep (D-09), pre-declared in 15-02-PLAN.md frontmatter
- `src/hooks/useAppState.ts` — WR-04 call site, sanctioned sweep (D-09)
- `src/components/SegmentEditor.tsx` — WR-05/WR-06 fixes, sanctioned sweep (D-09)

Any OTHER survivor = mechanical finding → record it and stop for triage per D-08. Also state explicitly that `package.json`/`package-lock.json` absent from survivors proves no dependency changes this milestone (supply-chain evidence for free).

**Assertion evidence:** Add an "## Evidence: catalog proofs (mechanical)" section quoting verbatim: the vitest verbose output lines for the four new data-integrity tests (`catalog contains exactly 45 conventions`, `spans exactly 11 categories`, `reachable via global-search id match`, `identical flat key sets`) plus a `grep -h '"category"' src/rules/*/*.json | sort | uniq -c` excerpt showing the 45 total. Use criterion-statement + verbatim-excerpt format from 12-SMOKE-MATRIX.md's PERF evidence sections.
  </action>
  <verify>
    <automated>grep -q 'Zero-code-diff proof' .planning/phases/15-milestone-verification-full-catalog-integrity/15-VERIFICATION.md && grep -q 'v1.4..HEAD' .planning/phases/15-milestone-verification-full-catalog-integrity/15-VERIFICATION.md && grep -q 'sanctioned sweep' .planning/phases/15-milestone-verification-full-catalog-integrity/15-VERIFICATION.md && grep -q '45' .planning/phases/15-milestone-verification-full-catalog-integrity/15-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 15-VERIFICATION.md contains the literal diff-filter command string `git diff v1.4..HEAD --name-only | grep -Ev '^(src/rules/|src/data/|src/locales/en\.json$|src/locales/fr\.json$)'` and its actual output
    - Survivor set in the output is exactly {src/lib/validation.ts, src/hooks/useAppState.ts, src/components/SegmentEditor.tsx} (± test files created by earlier phases' fix commits, each individually justified)
    - A justification bullet referencing D-06 "zero or justified" + D-09 exists per survivor
    - Section states package.json/package-lock.json are NOT in the survivor set (no dependency changes)
    - Catalog-proof evidence section quotes the four passing test names and the 45-total category count excerpt
  </acceptance_criteria>
  <done>Diff proof recorded with every survivor explicitly justified; catalog-proof evidence embedded verbatim.</done>
</task>

<task type="auto">
  <name>task 3: Author user-run smoke matrix script (D-02/D-03/D-04)</name>
  <files>.planning/phases/15-milestone-verification-full-catalog-integrity/15-SMOKE-SCRIPT.md</files>
  <read_first>
    - src/rules/teams/team-project.json, src/rules/sharepoint/spo-team-site.json, src/rules/power-platform/pp-environment.json, src/rules/purview/purview-sensitivity-label.json, src/rules/azure/azure-storage-account.json, src/rules/intune/intune-autopilot-device-name.json (confirm field names/library values match the literals below BEFORE finalizing the script)
    - .planning/phases/15-milestone-verification-full-catalog-integrity/15-CONTEXT.md D-01..D-04
  </read_first>
  <action>
Create `15-SMOKE-SCRIPT.md`: a step-by-step script the USER executes in the running app (`npm run dev`). Six workflows, each repeated across 4 axes: EN/Dark, EN/Light, FR/Dark, FR/Light (theme toggle via CommandBar sun/moon; language via EN/FR switcher) ≈ 24 cells. For EACH workflow specify: category to click, convention/pattern variant to select, LITERAL segment values to type/select, expected generated name, expected validation/governance behavior. Fixed values:

1. **Teams team** — Category "Teams" → "Project Team". Segments: Workload=`Atlas`, Function=`Finance`. Expected: `PRJ-Atlas-Finance`, mixed case preserved (re-proves CR-01 fix), ≤30 chars, all validation rows pass.
2. **SharePoint site** — Category "SharePoint" → "Team Site (Group-Connected)". SiteName=`sales-eu`. Expected: `sales-eu` lowercase-hyphenated, valid.
3. **Power Platform environment** — Category "Power Platform" → "Power Platform Environment". Lifecycle=`dev`, Region=`westeurope`, BusinessUnit=`IT`, Purpose=`sandbox`. Expected: `dev-westeurope-it-sandbox`.
4. **Purview label** — Category "Purview" → "Sensitivity Label", sublabel pattern variant. Classification=`Confidential`, Sublabel=`Payroll`. Expected: `Confidential-Payroll`, case preserved.
5. **Azure storage account** — Category "Azure" → "Storage Account". Workload=`app`, Environment=`prod`, Instance=`001`. Expected: `stappprod001` lowercase no separators. NEGATIVE probe (re-proves WR-02): clear Environment and Instance segments so the name reduces toward `st…` (< 3 chars) → the allowedPattern validation row must FAIL. If fields cannot be cleared in-app, substitute typing a custom 2-char value into a segment where allowCustomValue permits.
6. **Intune Autopilot device name** — Category "Intune" → "Autopilot Device Name", "Prefix + Random" pattern variant. Prefix=`LT-`, Macro=`%RAND:6%`. Expected template `LT-%RAND:6%` (11 typed chars ≤ 15 budget), validation passes. Boundary probe: select Macro `%RAND:12%` with Prefix `LT-` → exactly 15 expanded chars, still valid; verify no clamp misfires.

End the script with: instruction that the user judges each cell PASS/FAIL honestly (governance score correctness included); a blank results table with columns `| # | Workflow | EN/Dark | EN/Light | FR/Dark | FR/Light |`; note that results will be transcribed into 15-VERIFICATION.md (Plan 15-04).
  </action>
  <verify>
    <automated>grep -c '^| [0-9] \|PRJ-Atlas-Finance\|stappprod001\|LT-%RAND:6%\|dev-westeurope-it-sandbox\|sales-eu\|Confidential-Payroll' .planning/phases/15-milestone-verification-full-catalog-integrity/15-SMOKE-SCRIPT.md</automated>
  </verify>
  <acceptance_criteria>
    - 15-SMOKE-SCRIPT.md contains all six workflow headings naming their categories (Teams, SharePoint, Power Platform, Purview, Azure storage account, Intune Autopilot device name)
    - All six literal expected outputs appear: PRJ-Atlas-Finance, sales-eu, dev-westeurope-it-sandbox, Confidential-Payroll, stappprod001, LT-%RAND:6%
    - Contains the negative probes: storage <3-char minimum check (WR-02 re-proof) and Autopilot %RAND:12% boundary check
    - Contains the empty results table with columns EN/Dark, EN/Light, FR/Dark, FR/Light (24-cell matrix per D-01)
    - Every segment value referenced matches a real field/library option verified in read_first
  </acceptance_criteria>
  <done>Reproducible 6-workflow × 4-axis script ready for user execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell→report | Command output (gate logs, git diff listings) flows into a markdown artifact |
| registry→node_modules | `npm ci` installs packages from the lockfile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Tampering (script/content injection) | 15-VERIFICATION.md content generation | mitigate | All command output pasted inside fenced code blocks verbatim; never rendered as raw HTML, never passed through eval/xargs/sh -c with interpolated strings |
| T-15-03-02 | Tampering (command injection via crafted filenames) | git diff \| grep pipeline | mitigate | Fixed literal grep -Ev pattern; no xargs/eval/for-loop over filenames; output is only displayed and manually compared to the expected survivor set |
| T-15-03-03 | Supply-chain | npm ci | mitigate | No dependency changes this milestone; task 2's diff proof explicitly evidences package.json/package-lock.json absence from survivors; installs resolve from the committed lockfile only |
| T-15-03-04 | Repudiation (evidence fabrication/flake-hiding) | gate recording | mitigate | Verbatim output + stated exit codes required; known WSL vitest flake must be annotated and rerun-in-isolation documented, never hidden |
</threat_model>

<verification>
- All 5 gate commands exit 0 (recorded in report)
- Diff-filter survivor set == {validation.ts, useAppState.ts, SegmentEditor.tsx} (+ any earlier-phase fix-commit test files, individually justified)
- 15-SMOKE-SCRIPT.md complete with 24-cell results table scaffold
</verification>

<success_criteria>
Mechanical half of QUAL-01 fully evidenced in 15-VERIFICATION.md; smoke script ready; only the user-run matrix and verdict remain (Plan 15-04).
</success_criteria>

<output>
After completion, create `.planning/phases/15-milestone-verification-full-catalog-integrity/15-03-SUMMARY.md`
</output>
