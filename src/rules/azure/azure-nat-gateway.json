{
  "id": "azure-nat-gateway",
  "category": "Azure",
  "name": "NAT Gateway",
  "description": "Azure NAT Gateway naming per CAF (Microsoft Cloud Adoption Framework). NAT Gateway provides secure outbound connectivity for resources in a virtual network subnet. The name identifies the gateway associated with its subnet and resource group scope.",
  "pattern": "NGW-[Workload]-[Location]-[Environment]",
  "fields": [
    {
      "name": "Workload",
      "library": "azure/ngw-workload",
      "defaultValue": "SHARED",
      "allowCustomValue": true
    },
    {
      "name": "Location",
      "library": "core/location",
      "defaultValue": "AZ",
      "allowCustomValue": true
    },
    {
      "name": "Environment",
      "library": "core/environment",
      "defaultValue": "PROD",
      "allowCustomValue": true
    }
  ],
  "examples": ["NGW-SHARED-AZ-PROD", "NGW-HUB-AAD-DEV"],
  "builder": {
    "separator": "-",
    "defaultSegments": ["Workload", "Location", "Environment"],
    "availableSegments": ["Workload", "Location", "Environment", "Custom"],
    "recommendedSegments": ["Workload"],
    "lockedSegments": [],
    "fixedFirstSegments": [],
    "fixedLastSegments": []
  },
  "validation": {
    "maxLength": 80,
    "allowedPattern": "^(?!.*--)[A-Za-z][A-Za-z0-9-]{0,78}[A-Za-z0-9]$"
  }
}
