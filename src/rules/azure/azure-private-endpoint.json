{
  "id": "azure-private-endpoint",
  "category": "Azure",
  "name": "Private Endpoint",
  "description": "Azure Private Endpoint naming per CAF (Microsoft Cloud Adoption Framework). Private Endpoint secures private connectivity to PaaS services over the virtual network. The name identifies the endpoint within its resource group and subscription scope for network governance.",
  "pattern": "pep-[Service]-[Workload]-[Environment]",
  "fields": [
    {
      "name": "Service",
      "library": "azure/pep-service",
      "defaultValue": "KV",
      "allowCustomValue": true
    },
    {
      "name": "Workload",
      "library": "azure/pep-workload",
      "defaultValue": "SHARED",
      "allowCustomValue": true
    },
    {
      "name": "Environment",
      "library": "core/environment",
      "defaultValue": "PROD",
      "allowCustomValue": true
    }
  ],
  "examples": ["pep-kv-shared-prod", "pep-sql-data-dev"],
  "builder": {
    "separator": "-",
    "defaultSegments": ["Service", "Workload", "Environment"],
    "availableSegments": ["Service", "Workload", "Environment", "Custom"],
    "recommendedSegments": ["Service"],
    "lockedSegments": [],
    "fixedFirstSegments": [],
    "fixedLastSegments": []
  },
  "validation": {
    "maxLength": 50,
    "forceLowercase": true,
    "allowedPattern": "^(?!.*--)[a-z][a-z0-9-]{0,48}[a-z0-9]$"
  }
}
